# 更新日志

## v0.43.17 - 2026-08-28

### 修复

- 修复发布门禁要求的 Rust 格式化问题，确保跨平台桌面构建可以继续执行。

## v0.43.16 - 2026-08-28

### 修复

- P2P 局域网直传改为聊天输入区显式入口：点击后先与一对一私聊对象握手，成功后才选择文件；普通消息和普通附件不再自动通过 LAN 发送。
- 修复大文件原生上传路径，改由 Rust 从磁盘流式提交给 Rocket.Chat，并正确透传服务器大小限制错误。
- 旧版 LAN 控制消息不再进入聊天内容，避免 Key 或内部控制数据出现在会话中。

## v0.43.15 - 2026-08-27

### 修复

- 全局搜索同时命中多人会话和用户名时，进入该多人会话会自动打开成员面板，定位并高亮命中搜索词的成员（issue #364）。
- 一对一私聊和多人会话也能 @ 不在会话里的人：目录搜到的会话外候选标注「不在会话中」，只插入提及文本、不邀请（对 DM 邀请会另建新会话），对方不会收到 @ 提醒；群聊的「非群成员、发送前自动拉群」行为不变。

### 验证

- `pnpm test:pure`：237 passed；全量回归：992 passed；Playwright UI 111 passed、3 skipped；Rust `cargo test --locked`：162 passed、4 ignored；Web build、typecheck、ecosystem 和 architecture checks 通过；顺带修复 `scripts/test-app-ecosystem.mjs` 在 Windows 上 GNU tar 把盘符冒号误判为远端主机的问题。

## v0.43.14 - 2026-08-23

### 改进

- 完成 Issue #356 本轮架构收敛：Rust 原生宿主按进程、Codex/DSH runtime、LAN 发现与文件传输职责拆分，保留 Tauri command、生命周期和 LAN wire protocol 兼容 facade。
- Rocket.Chat client 将认证、用户、房间、消息、文件、搜索、偏好和 emoji endpoint 分域，共用统一 request context 与 capability snapshot；公开 `RcRestClient` API 不变。
- 本地数据增加 schema 注册、版本迁移计划与 dry-run、备份/恢复和失败回滚，未知 key、未来版本和 scope 冲突显式拒绝。

### 验证

- Rust `cargo test --locked`：162 passed、4 ignored；`pnpm test:pure`：237 passed；全量回归：989 passed；Playwright UI 111 passed、3 skipped；Native lifecycle、Web build、typecheck、ecosystem 和 architecture checks 通过。

## v0.43.13 - 2026-08-23

### 新功能

- 应用启动、Kernel、聊天、Codex/DSH 管家和桌面原生宿主改用统一的生命周期边界，账号切换、退出和运行时崩溃后的资源回收更可控。
- Rocket.Chat 客户端补充能力协商、按领域访问入口和实时网关；待办、日历、偏好与通知数据增加版本、账号作用域、损坏隔离以及备份恢复能力。
- 恢复 Windows、macOS 和 Linux 三平台交付的架构护栏与发布前验证，继续保持现有 IPC、Rocket.Chat 和存储键兼容。

### 修复

- 修复设置页在领域客户端调用中丢失运行时上下文导致偏好加载失败的问题。
- 修复浏览器端待办数据升级为版本 envelope 后 UI 读取旧数组格式的兼容问题。

## v0.43.12 - 2026-08-22

### 修复

- 打开 RocketX 的 DSH 管家页面时，支持 `--no-open` 的 DSH 运行时不再额外打开系统浏览器主页；旧版运行时继续使用兼容参数。
- 消息正文中的 UNC 共享路径改走桌面原生命令并校验主机、共享名和路径格式，修复 #357 的路径打开问题。

## v0.43.11 - 2026-08-22

### 新功能

- 团队配置支持共享联系人备注；个人已有备注优先，团队配置只补齐缺失项。

### 修复

- 局域网文件卡片改用桌面原生命令打开，并校验本地文件路径，修复打开权限不足。
- SMB 共享目录更新移出 UI 线程，修复 Windows UNC 路径大小写导致的误拒绝，并为启动前失败保留更新结果。
- Desktop CI 产物上传收窄到实际 bundle 目录，避免递归扫描 DSH 运行时依赖产生错误注释。

## v0.43.10 - 2026-08-21

### 新功能

- 专注模式（daily-loop 规格 v1，`docs/specs/daily-loop.md`）：导航栏一级入口发起专注会话（25/50/90 分钟或不限时，可延长、可提前结束）。专注期间通知强制走聚合规则——禅模式的穿透白名单（@我、私聊、关键词、P1）照常生效，任务栏停止闪烁；在线状态自动切为忙碌，结束或到点后恢复进入前的状态。结束时弹出消化卡片，报告本次攒下的消息数、涉及会话数与穿透条数，可一键去消息处理。应用重启视为会话结束，状态由启动时的状态偏好自愈。
- 备注名导入导出：设置 → 侧栏可把联系人备注导出为 JSON 备份或迁到别的设备，导入只补缺、不覆盖已有备注。文件只含人的备注（`u:` 键；会话备注的 rid 换服务器即失效，继续走账号同步通道，不进导出文件），导入旧版整表导出文件时会话备注自动跳过并提示条数。
- 超长消息折叠：超过半屏（按当前窗口高度折算）的消息默认折叠成预览，点「展开全部」查看全文，可再收起。设置 → 消息 提供开关（默认开）与折叠时机（半屏/一屏/两屏），随账号跨设备同步。

## v0.43.9 - 2026-08-21

### 修复

- Windows 安装器/卸载器在替换文件前先清扫仍从安装目录运行的进程（按可执行路径匹配，最多等待 10 秒），修复旧版卸载时杀进程超时或 full 私有运行时（node.exe 等异名进程）占用安装目录导致的「Unable to uninstall」升级失败。

## v0.43.8 - 2026-08-20

> `v0.43.7` 候选因发布文档门禁（compatibility matrix 版本句未同步）失败而停止发布；不可变标签保留为未发布候选，正式 Latest 从本版本继续。

### 改进

- 通知提示音功能落地：新消息弹出通知时按「提示音音量」设置播放短促提示音（WebAudio 合成，无音频资源依赖），通知被抑制或音量为 0 时不发声；移除从未生效的 `convertAsciiEmoji`/`hideUsernames` 残留偏好键。
- App/插件桥的 `chat.postMessage` 不再对超长文本直接报错，改为与用户输入框一致的按 `Message_MaxAllowedSize` 自动分段顺序发送。
- 局域网文件直传增加可配置大小阈值（默认 50 MB，设置页可选 10/50/100 MB、1 GB 或「任何文件」）：小文件直接走 Rocket.Chat 上传，省去 P2P 握手开销；首次直传成功给一次性说明，之后静默自动路由。
- 消息文本中的 UNC 共享路径（`\\主机\共享\...`）渲染为可点击卡片，点击弹确认框（明确提示将连接的局域网主机）后用系统默认程序打开；仅桌面端，Web 端提示不可用。

### 修复

- 文件上传补上客户端预检与零拷贝：超过服务器 `FileUpload_MaxFileSize` 的文件在发送前直接提示上限值（不再先读进内存再被服务端拒绝），multipart 改为 Blob 直传消除两倍内存拷贝，服务端超限错误会透出明确提示。（#355）
- SMB 共享目录的自动更新：更新 helper 改用独立文件名 `rocketx-update-helper.exe`，不再被 NSIS 的进程检查按主程序映像名误杀；安装启动失败（如取消 UAC 授权）与安装器真实退出码分开报告并附输出摘要，不再笼统报「退出码 1」。（#354）
- 应用内 AI 会话的 `rocketx_business` 房间数据工具注入修复：ADO 配置同步失败且凭据已清空时，凭据门闩不再永久关闭，工具按能力降级返回 not_configured 而不是整体消失。（#309）
- 局域网直传的断网兜底修复：Rocket.Chat 上传失败（断网/5xx）或文件超上传上限时无视大小阈值自动改用局域网直传重试，恢复断网时小文件互发能力；阈值只管在线时的选路。
- 局域网直传的文件消息在收发双侧都显示「局域网直传」标记（接收方此前误显示「待补发」）；文件从未经过服务器，引用、转发、多选合并转发、置顶、复制消息链接等依赖服务端的操作对这类消息不再开放。
- Windows full 包启动提速：DSH/Codex 运行时探测移出主线程（探测期间窗口不再冻结）并并行化；内置 DSH 运行时归档的校验哈希按文件大小与修改时间缓存，不再每次启动全量 sha256；首屏渲染不再等待 AI 运行时探测完成，探测就绪后再初始化内核。
- 服务器版本提示 `2015` 不再被错误归入 legacy（api-version 5.0）：TFS 2015 只接受 1.0，现在会正确映射并通过真实连接验证；2017/2018 同时独立成档（3.0/4.1，按微软支持矩阵，未经真实服务器验证）。
- 会话列表右键菜单不再被程序化滚动（在线状态/未读刷新引起的列表自滚）瞬间关闭；用户滚动滚轮或触摸滑动仍会正常关闭菜单。
- 设置页的在线状态按钮在自动离开等外部状态变化时实时同步高亮。
- 清理前端从未调用的 Tauri 命令（`butler_todo_get`、`butler_todo_overdue`、`codex_agent_workspace`、`lan_identity_get`）。

## v0.43.6 - 2026-08-19

### 改进

- 发送超过服务端 `Message_MaxAllowedSize` 的长文本时自动按段落拆分成多条顺序发送，不再直接报“过长”；引用前缀保留在第一段，线程回复语义不变，中途失败会停止后续分段并提示。（#349）
- 私聊（含多人私聊）恢复 @ 提及：@ 按钮与输入候选可用，但不会触发全局目录搜索，也不再提供“通知所有人/通知在线成员”广播项。（#353）
- 设置页新增「导入历史备注」：换服务器地址（如 IP 变化）后被账号隔离归档的旧备注可以一键合并回来，已有的同名备注不会被覆盖，导入后同步到服务端。（#350）
- 系统已安装的 DSH 版本校验从精确匹配 0.1.0-rc.6 放开为 semver 最低版本校验（>= 0.1.0-rc.6，兼容 `1.0.0rcalpha07` 这类无连字符写法）；高于验证版本的构建只告警不阻断。bundled 运行时仍精确锁定 0.1.0-rc.6。（#352）

### 修复

- 设置页「自动离开」开关补上执行逻辑：无操作超过设定时长（下限 30 秒）自动把状态置为「离开」，恢复操作后回到「在线」；只在当前状态为「在线」时生效，自动离开期间手动改过的状态不会被回滚，也不会被记为下次启动的显式状态偏好。
- 反向 MCP 新增只读工具 `rocketx_read_attachment`，外部 AI 工具可凭站内 `/file-upload/` 路径读取附件原图（图片以 base64 image content 返回，上限 10MB，拒绝站外 URL 与目录穿越）。（#347）
- 备注名改为随账号同步到 Rocket.Chat 用户偏好，重装或换设备登录后依然有效；localStorage 仅作缓存，首次登录自动把本机已有备注迁移上传。（#350）
- 消息通知等偏好选项增加本机镜像兜底：成功写入服务端的修改会记录到本机，重启后服务端读取失败时不再静默回退默认值。（#351）
- 用户偏好读写适配 Rocket.Chat 8.6：写入优先走 DDP `saveUserPreferences`（REST `users.setPreferences` 在 8.6 拒绝自定义键），读取改用 `users.getPreferences`（8.6 的 `users.info` 不再返回偏好）；旧版本服务器走回退路径不受影响。（#350, #351）
- Azure DevOps Server 的 PowerShell 脚本在 Windows PowerShell 5.1 下不再吞掉真实错误：预加载 `System.Net.Http` 程序集，401 等服务器响应能正常透出为可读诊断。
- 服务器版本提示 `2015` 不再被错误归入 legacy（api-version 5.0）：TFS 2015 只接受 1.0，现在会正确映射并通过真实连接验证。

## v0.43.5 - 2026-08-17

### 修复

- DSH 房间会话与共享托管不再把 `DEEPSEEK_API_KEY` 当作唯一启动门禁；RocketX 沿用 DSH 当前选择的 provider、模型和凭据，并在真实调用失败时展示原生诊断。
- 私人房间 DSH 会话在控制器中断后会清理失效的审批与问题，同一原生会话可重复聚焦；房间 AI 打开时不再遮暗或阻断主聊天，也不重复显示悬浮按钮。
- 对话中的 Markdown、表格与代码块会使用容器可用宽度；窄窗口仍只在宽内容内部横向滚动。
- 共享 AI 的超长回复会读取 Rocket.Chat 的 `Message_MaxAllowedSize` 并顺序分段，保留 Unicode、代码围栏、列表与缩进语义；机器人已发送部分内容后不会切换为宿主账号继续发送。
- Debug 构建不再自动检查正式发布源，避免开发窗口出现生产版升级提示；正式版的版本比较规则保持不变。

## v0.43.4 - 2026-08-17

### 修复

- 修复 Unix 平台发现 npm/pnpm 全局 DSH 时重复使用 `HOME` 路径导致的 Rust 编译错误，恢复 macOS universal 与 Linux 发布构建；Windows Slim、MSI、Full 的 DSH 发现与卸载保护语义保持不变。
- 私人房间 DeepSeek 改用独立临时目录；缺少 API Key 时可从错误页直接打开 DSH 配置，不再要求进入管家工作区。
- 新设备没有托管项目时，可在 AI 托管开启面板直接选择本地目录并添加项目，不再跳转到另一个页面寻找入口。
- 对话中的富 Markdown、AI 托管回答和 Codex transcript 使用更宽的文档区域；普通正文保持可读列宽，表格和代码块可利用宽屏并在窄窗口横向滚动。

## v0.43.3 - 2026-08-17

### 修复

- 桌面端统一支持 HTTP 与 HTTPS Rocket.Chat 连接，不再只放行单一传输方案。
- HTTPS 连接改用系统证书信任链校验，不再通过关闭证书校验来放行桌面登录。
- 桌面登录移除额外的 WebView Origin 头，避免与服务端传输和鉴权策略冲突。
- 登录错误改为按网络、服务端响应、证书与凭据阶段分类，并在界面上脱敏显示可操作的诊断详情。
- Slim 安装包补齐 pnpm 全局 DSH 的发现，并在系统 DSH 缺失时给出安装固定版本或改用 Full 安装包的明确提示；卸载或换装只清理 RocketX 私有运行时，不触碰 npm/pnpm 全局 DSH。

## v0.43.2 - 2026-08-17

### 改进

- 管家与私人房间 AI 的流式 Markdown 改为稳定的渐进块渲染，吐字和完成时复用同一条消息节点，不再先显示原始 Markdown、再整篇重排。
- 管家中的共享托管项与房间托管侧栏继续引用同一条 session，支持打开房间、恢复和结束；私人房间 AI 保持用户独享，不与共享托管混用。
- 管家页将共享托管按项目折叠分组，并从个人项目历史中去掉同一条托管线程；房间侧栏和输入区的宽度、字号与层级也重新收紧。

### 修复

- 共享 AI 托管继续按房间成员权限开放，而不是只允许全局管理员、机器人或房间管理角色。
- 可读的托管正文只用于展示；跨客户端宿主仲裁只接受 RocketX 正式开启流程写入的租约消息标识或自定义字段，普通成员手工复制相同文案不会占用托管。
- 启动级 Codex / DSH / 无 AI 选择、`@ai` 候选与共享托管测试夹具继续对齐，避免把 Web 默认无 AI、私人会话和共享托管混在一起。
- 回归测试补上 shared session identity、启动入口、停机、滚动和 UI 流程覆盖。

## v0.43.1 - 2026-08-16

### 修复

- Web 端在没有本地 Codex / DSH 执行引擎时，继续保留共享 AI 托管、`@ai` 候选和同一套信息架构；只禁用本地执行，不再错误默认激活某个后端。
- 共享托管状态卡改为纯可读正文，原版 Rocket.Chat 不再显示 HTML 注释或 URL 编码租约 JSON；RocketX 继续兼容旧标记卡片，但只有经服务端确认的全局管理员、机器人账号、群主、群管理员或负责人发布的短期租约才能参与宿主仲裁，普通房间文字不再能伪造托管状态。
- UI 测试夹具显式选择 Codex 或 DSH 执行引擎，避免把“网页端默认无 AI”与需要本地运行时的场景混在一起。

## v0.43.0 - 2026-08-16

### 核心改造

- 桌面 AI 改为启动级 Codex / DSH / 无 AI 三选一，保存后重启生效，同一进程不热切换也不双开；slim 首次按本机可用性自动选择，两者都有时优先 DSH，Windows full 先验证固定内置 DSH，损坏或缺失时再按实际可用性回退 Codex 或无 AI。管家、私人房间 AI、共享 AI 托管和消息交接统一继承该选择。
- DeepSeek 管家直接在 RocketX 外壳中安全嵌入官方 DSH Web；DSH 原生页面继续负责私人会话、模型、Agent、权限和凭据，RocketX 只管理进程、loopback 宿主、房间入口与共享托管 controller。
- 明确分离私人房间 AI 与共享 AI 托管：私人会话按服务器、账号和房间隔离且不向群发送消息；共享托管仍对房间成员可见，并在房间侧栏与管家全局视图引用同一条 Codex Thread / DSH Session，不复制 transcript，也不重复启动 AI。

### 新增

- 管家统一展示共享托管会话的房间、项目、状态和当前任务，并提供打开房间、恢复与结束；已结束会话以只读方式读取原 Harness 历史。
- 开启共享托管时可为本次会话选择 Codex 模型、推理与权限，或从 DSH 原生目录选择模型、推理、Agent 与权限；配置随原生会话保存和恢复，不覆盖 Codex 管家默认值，DSH 显式模型选择遵循上游的后续默认语义。
- Composer 与话题回复新增原生 `@ai` 候选，仅在当前作用域存在有效共享托管时出现，并复用鼠标、方向键、Enter、Tab 与 Escape 交互。
- 默认贴纸扩充为 21 张静态 Twemoji 与 3 张 Noto 动图；个人导入、上传和接收链路保留 GIF MIME 与原始动图字节。

### 改进

- 无说明文字的内置贴纸使用带所有权标记的 Rocket.Chat 自定义表情 shortcode，让原版 Rocket.Chat 也能显示透明贴纸和 GIF；无管理权限或命名冲突时回退现有图片附件路径。
- AI 托管完整回答使用更接近 Codex 的宽版 Markdown 文档流，私人房间 AI 保持自然对话形态并提供逐条复制；流式阶段继续使用稳定纯文本，避免吐字闪烁。
- 移除 RocketX 自绘 DSH 设置与会话副本，凭据按钮和配置能力回到官方 DSH Web；全局交接在上游尚无输入注入协议时复制提示词并打开原生页面。
- 首屏后空闲加载拼音字典，非首屏页面和房间面板按需加载，减少启动静态依赖与首屏阻塞。

### 修复

- DSH 改为单 Host、多 Session、多连接租约，并安全迁移旧单体 session frame，避免多个 writer 造成 `seq gap` 与损坏会话日志。
- 多设备同时声明同一共享托管时按稳定规则选出唯一宿主；有效远端租约会阻止本机旧会话重复恢复或再次响应 `@ai`。
- DeepSeek 托管附件进入 DSH 暂存路径，不再误调 Codex 附件命令；DSH 子进程过早退出时展示真实诊断。
- 房间 AI 面板宽度受外层对话框约束，关闭后重开继续使用同一条私人原生会话。

### 发布

- 本版本继续通过受保护工作流交付 Windows x64 slim、MSI 与 full，macOS universal，以及 Linux x64 安装包、更新签名、插件包和 SHA256 校验文件；Windows updater 继续指向 slim 安装包。

## v0.42.7 - 2026-08-15

### 修复

- 从管家把既有会话交给 Codex App 前会停止 RocketX 的共享 app-server，并以统一操作租约等待连接、定时任务、附件发送、会话创建和管理操作结束；停止运行时到派发 deep link 全程保留所有权，避免 Codex App 提示会话正被其他应用使用。（#338）

### 发布

- 本版本继续通过受保护工作流交付 Windows x64、macOS universal 与 Linux x64 安装包、更新签名、插件包和 SHA256 校验文件。

## v0.42.6 - 2026-08-15

### 修复

- DeepSeek 管家页的配置入口改为始终可见的弹窗，并在页头展示当前提供方、模型和推理强度；历史会话、新会话及配置刷新期间只展示会话实际使用的模型，且模型元数据读取不再阻塞首条消息。（#334）

### 发布

- 本版本继续通过受保护工作流交付 Windows x64、macOS universal 与 Linux x64 安装包、更新签名、插件包和 SHA256 校验文件。

## v0.42.5 - 2026-08-15

### 新增

- 上传的 HTML / HTM 附件可在禁用脚本、同源、表单、弹窗与导航权限的严格沙箱中预览；浏览器可解码的常见视频附件经认证读取后使用原生控件播放。（#318）

### 改进

- 普通消息、AI 对话、定时任务结果、Markdown 文件与 Codex Artifact 统一使用完整 Markdown 排版，并分别保持紧凑聊天与宽松文档密度。（#319）
- Azure DevOps 六种层级模板的说明和预览改为从当前过程的真实工作项类型与实际创建结构推导，兼容 Basic、Agile 与自定义层级。（#325）
- AI 管家的共享状态与审批文案保持后端中立；业务入口交给 Codex 时只临时切换当前视图，不覆盖用户显式保存的 Codex / DeepSeek 默认偏好。

### 修复

- AI 管家的 Codex 主任务、房间 Codex 与 DeepSeek 会话会合并高频回答和任务过程的界面刷新；未完成内容保持纯文本，完成后再解析 Markdown，避免吐字时刷屏闪烁，同时不丢失任何分片。
- 图片灯箱下载会在主链接缺失时回退到图片地址，并在没有有效路径时停止无效下载。（#324）
- DSH 业务 MCP 名称超过 32 个字符时会确定性缩短并附加哈希后缀，避免会话启动失败。（#327）

### 工程

- 发布前协议检查使用仓库固定的 Codex CLI `0.144.4`，并清理既存 Rust 格式漂移；对应门禁均恢复通过。

### 发布

- `v0.42.4` 候选在公开前发现上述 AI 管家流式输出问题，因此停止发布；不可变标签保留为未发布候选，正式 Latest 从本版本继续。
- 本版本继续通过受保护工作流交付 Windows x64、macOS universal 与 Linux x64 安装包、更新签名、插件包和 SHA256 校验文件。

## v0.42.3 - 2026-08-14

### 新增

- 管家新增独立的 DeepSeek 视图，首次默认打开 DSH 视图并保留既有偏好；直接使用 DSH 的原生会话、历史、模型与提供方、推理强度、Agent preset、权限 preset、审批、问题和凭据能力；房间与话题中的 AI 托管也可以按会话选择 Codex 或 DeepSeek。

### 改进

- 桌面交付改为分层：默认 slim 只探测已安装的 Codex / DSH，不随包携带这两个运行时；系统 DSH 只有在 RocketX 验证为 `0.1.0-rc.6` 时才可用，后续版本需先升级支持线；Windows full 额外把固定的 `@openai/codex@0.144.4`、`@deepseek-ai/dsh@0.1.0-rc.6`、私有 Node 与 OCR 安装到 `%LOCALAPPDATA%\RocketX\resources`，macOS 和 Linux 官方包当前仍是 slim-only。slim updater 不会更新 full 的私有资源。

### 修复

- DSH 固定运行树改为 Windows full 私有资源中的单归档分发，并在首次使用时解压到 RocketX 应用数据目录；默认 slim 不再携带这类资源，避免 Windows MSI 超过 32,767 个文件，也避免 Linux AppImage 的 `linuxdeploy` 扫描 DSH 内部原生库。
- DSH 进程断开时会清理失效的审批、问题和排队消息，并把仍在运行的会话标记为中断，避免旧卡片继续可操作或界面假装任务仍在执行。
- `v0.42.0` 与 `v0.42.1` 候选均未公开发布；本版本补齐三平台打包门禁后再交付完整安装包。

### 发布

- 本版本继续通过受保护工作流交付 Windows x64、macOS universal 与 Linux x64 安装包、更新签名、插件包和 SHA256 校验文件。

## v0.42.1 - 2026-08-14

### 新增

- 管家新增独立的 DeepSeek 视图，直接使用 DSH 的原生会话、历史、模型与提供方、推理强度、Agent preset、权限 preset、审批、问题和凭据能力；Codex 视图继续保留 Skills、Plugins、Apps、Memory 与 Codex App 接续，两套后端各自展示真实能力。
- 房间与话题中的 AI 托管可以在创建会话时选择 Codex 或 DeepSeek，并分别保存原生 Thread ID 或 DSH Session ID；旧会话继续按 Codex 恢复，不做隐式跨后端迁移。
- DeepSeek API Key 可在 DSH 视图内写入其私有 `DSH_HOME` 凭据文档；模型、Agent 和权限选择直接调用 DSH 原生 RPC，不在 RocketX 中复制一套通用配置或 Provider 管理层。

### 改进

- 当时的正式桌面安装包候选固定携带官方 `@deepseek-ai/dsh@0.1.0-rc.6` 的完整生产运行树，不再要求用户另行下载 DSH 或保留 `deepseek-harness` 源码仓库；这一口径后来被 v0.42.3 的 slim/full 分层取代，当前只对 Windows full 保留私有运行时。
- Codex 与 DeepSeek 使用两套明确的视图和控制器，共用的只有 Rocket.Chat 托管租约、消息上下文、状态与回帖，不再为了后端可换而增加通用 Runtime 抽象。

### 修复

- DSH 进程断开时会清理失效的审批、问题和排队消息，并把仍在运行的会话标记为中断，避免旧卡片继续可操作或界面假装任务仍在执行。
- 修复 Desktop Build 在三平台 Rust 单元测试前未准备固定 DSH 运行时、导致 `v0.42.0` 候选标签无法完成构建的问题；该候选未公开发布。

### 发布

- 本版本继续通过受保护工作流交付 Windows x64、macOS universal 与 Linux x64 安装包、更新签名、插件包和 SHA256 校验文件。

## v0.41.1 - 2026-08-13

### 改进

- 工作台的“贡献概览”更名为“我的代码”，更准确地表达该页面展示当前用户代码活动的用途。（#311）

### 修复

- 首次开启 AI 托管时会立即打开进度面板，冷启动期间不再表现为界面卡住。（#310）
- 全局快捷键重新注册会等待上一轮注册完成并释放，避免偶发报告快捷键被应用自身占用。（#312）

### 发布

- 本版本继续通过受保护工作流交付 Windows x64、macOS universal 与 Linux x64 安装包、更新签名、插件包和 SHA256 校验文件。

## v0.41.0 - 2026-08-12

### 改进

- “我的贡献”更名为“贡献概览”，默认显示最近一年的当前 ADO 用户创建 PR；查询继续使用服务端时间范围、每项目分页、有限并发、缓存和取消，避免扩大范围后形成无界扫描。（#307）
- Windows 主窗口隐藏、最小化或失焦时使用 WebView2 官方低内存目标，重新显示或聚焦时恢复正常目标；同口径 Release 实测中，总工作集约下降 13%，GPU 子进程工作集约下降 17%。（#306）

### 修复

- 正式安装版的开机启动项会刷新为当前安装路径并带 `--autostart`，系统登录后静默驻留托盘；Debug 与任意本地 `debug` / `release` 构建不会读取、创建或覆盖系统启动项。（#276）
- 打开会话后持续校验多帧布局变化，图片、卡片等异步内容在后续帧撑高时仍会滚到最新消息，同时保留用户主动阅读历史和消息定位意图。（#299）
- 共享目录更新不再调用未授权的 `plugin:process|exit`；目录清单可省略 Minisign 签名，但安装器会在探测、主进程交接和 helper 安装前以固定 SHA-256 重复校验，GitHub/HTTP 更新仍强制签名。（#304）
- 首次引导在深色主题下改用稳定的深色语义令牌，标题、正文、图标和焦点状态不再出现浅色背景上的低对比文字。（#305）

### 发布

- 本版本继续通过受保护工作流交付 Windows x64、macOS universal 与 Linux x64 安装包、更新签名、插件包和 SHA256 校验文件。

## v0.40.4 - 2026-08-12

### 修复

- AI 管家执行任务时仍可打开并使用房间 Codex，会话切换不再被旧的全局繁忙判断阻断，后台管家任务继续保留运行状态和输出。（#285）
- 不同房间可以同时运行各自的 Codex 会话；切换房间不会停止、覆盖或串入其他房间的任务状态。（#285）

### 发布

- 本版本继续通过受保护工作流交付 Windows x64、macOS universal 与 Linux x64 安装包、更新签名、插件包和 SHA256 校验文件。

## v0.40.3 - 2026-08-12

### 改进

- “我的贡献”只统计当前 ADO 用户创建的拉取请求，并按项目分页、限流和去重；移除提交、工作项与仓库全量扫描，降低 Azure DevOps Server 压力并缩短加载时间。（#298）
- 统一界面字号令牌并清理任意像素字号，提升小号辅助文字的可读性，同时保留六档桌面缩放。（#135）
- Codex app-server 冒烟检查改为复用正式握手断言，避免测试协议与运行时协议逐渐分叉。（#285）

### 修复

- 转发含原图链接的消息时优先重新上传原文件，不再错误使用缩略图地址。（#301）
- 原生更新器不再提示同版本或旧版本；点击更新时重新获取有效资源，下载或重启失败时也不会残留永久加载提示。（#300）

### 发布

- 本版本继续通过受保护工作流交付 Windows x64、macOS universal 与 Linux x64 安装包、更新签名、插件包和 SHA256 校验文件。

## v0.40.2 - 2026-08-12

### 新增

- 管家可以在同一 Codex Runtime 中同时运行多个独立任务与审核；每个线程分别接收输出、审批和输入，刷新或停止其中一个任务不会打断其他任务。
- 工作台新增“我的贡献”，聚合当前 ADO 身份最近两个月的提交、PR 与工作项活动；热力图保持紧凑，明细可追溯，并在服务器能力或扫描上限导致覆盖不完整时明确说明。（#291）
- 看板工作项可以直接交给 AI 处理并带入真实工作项上下文；PR 行默认打开 ADO，AI 审查保留为独立动作。（#292、#296）

### 改进

- 本机 AI 项目统一在管家中管理，团队共享配置与个人执行目录不再混在同一设置页；聊天托管入口按项目名称选择，旧本地目录会安全迁入项目列表。
- 管家消息的标题、列表、代码、表格、引用、链接和段落节奏向 Codex App 对齐，并统一字体层级与宽窄布局。
- 团队配置可直接粘贴受保护的 Azure DevOps 文件链接，也可从 UNC 共享读取；复用当前 ADO 登录或 Token，并兼容 Bearer 认证，不把凭据写进配置文件。
- Codex 运行时探测会逐个检查版本、`app-server` 能力和登录状态，失败后继续选择可用候选；设置页可查看并复制脱敏诊断，手动路径失败时可一键恢复自动检测。（#287）
- 新安装的桌面端默认启用通知与开机启动；自启动保持后台隐藏，普通启动和二次打开仍会显示主窗口。桌面缩放可调并在重启后保留。（#295）
- 共享目录更新会在校验签名和安装类型后自动接管、安装并核对新版本，失败结果在重启后明确显示。（#284）

### 修复

- 贡献页默认只查询最近两个月，并对分页、项目、仓库和 PR 线程扫描设置有界并发与安全上限，避免页面长时间加载或对 IIS 形成请求风暴。
- 启动和已安排任务不再阻塞首屏；到期任务按队列执行，手动编辑项目路径也不会触发 React 重复更新。（#294）
- 打开会话、跨房间定位消息和异步内容撑高时会遵循当前滚动意图，首次进入默认看到最新消息，用户正在阅读历史时不被强制拉回底部。（#143）
- 未知线程的宿主审批请求会立即拒绝，不再挂住当前任务；团队配置来源与本机路径继续按作用域隔离，避免跨账号残留或泄露。

### 发布

- 本版本继续通过受保护工作流交付 Windows x64、macOS universal 与 Linux x64 安装包、更新签名、插件包和 SHA256 校验文件。

## v0.40.1 - 2026-08-11

### 改进

- 图片发送确认弹窗支持聚焦编辑多行说明，并按主输入框的回车偏好发送；最终说明与图片、回复关系保持在同一条消息中。（#283）
- 房间 AI 在首段输出前持续显示“正在思考”，管家占用运行时时给出准确状态，不再误报“另一个 Codex 正在运行”。（#285、#290）

### 修复

- 管家流式输出不再逐分片重建 Markdown，并关闭消息区浏览器滚动锚定，避免输出时页面上下跳动；同时移除输入区常驻的 AI 托管横幅。（#282）
- 仅宿主托管模式下静默忽略非宿主指令，不再向房间广播“当前 Agent 会话仅宿主可指挥”。（#288）
- 引用普通消息、卡片或文件时，即使 Rocket.Chat 把正文放在嵌套附件中，也会显示可读的引用内容而不只显示用户名。（#289）

### 发布

- 本版本继续通过受保护工作流交付 Windows x64、macOS universal 与 Linux x64 安装包、更新签名、插件包和 SHA256 校验文件。

## v0.40.0 - 2026-08-10

### 核心改造

- 管家全面切换到 Codex app-server 原生任务模型：直接使用原生 Thread、模型、推理强度、权限、Steer / Queue、刷新与中断语义，不再维护第二套自定义 AI 对话运行时。
- 插件页直接投影 Codex 的 Plugins、Skills 与 Apps 目录；长期记忆改由 Codex Memory 维护，删除 RocketX 自建的画像、学习、Memory 注入和模型 Provider 路由。
- 保留 RocketX 外层导航、消息与确定性工作台；聊天中的共享 Agent、已安排任务和业务能力统一经 Codex 与 Skills 执行。
- 工作区明确分为系统临时会话、系统管家会话和用户托管项目：普通聊天与房间管家使用系统目录，重量级 AI 托管使用独立项目及独立模型、推理设置。

### 改进

- 管家界面按 Codex App 的任务列表、工作区、原生输入卡、审批卡和 Composer 交互重做，并补齐移动端任务抽屉与无横向溢出门禁。
- 已安排任务支持 Codex 原生计划文件、固定时间、时间窗口内按间隔执行、Heartbeat 原线程续接，以及当前页面创建、启停、立即运行和多次运行历史。
- 共享 Agent 与管家任务统一提供“拒绝”“允许一次”“本次任务允许”三档审批；聊天托管可独立选择权限档并默认使用“替我审批”。
- HTML、图片及本地文件结果可在会话内打开 Artifact 预览面板，并支持查看源码、复制、定位文件和交给系统应用或浏览器打开；预览时自动收起项目栏以释放空间。
- 房间内 Codex 改为可持续对话面板，重新打开自动回到最新消息，并提供明确的新会话入口；项目树默认折叠较旧任务并保留重命名、归档和项目移除操作。
- 新增功能规格、能力矩阵、验收映射与文档生命周期标记，历史方案明确标注为 superseded 或 archive。

### 修复

- 网页版不再误调用 Tauri 目录选择器或本地 Codex 命令，明确提示本地执行面仅在 RocketX 桌面端可用；消息与确定性功能保持可用。
- Codex Turn 被中断或 app-server 意外退出时不再显示为成功，运行中事件会标记失败，同时保留已生成的部分结果并提供原线程刷新恢复入口。
- 定时任务变更必须经过当前用户显式审批；后台自动化和共享托管不能静默创建、修改、删除或运行计划任务。原生文件写入失败时回滚本地状态，避免两边分叉。
- 定时任务在未登录、工作区缺失、Skill 停用或 Runtime 失败时记录错误并推进当日触发游标，避免每分钟重复执行同一失败任务；Heartbeat 会按官方 `target_thread_id` 格式恢复目标会话。
- 修复 Windows 开机启动项不能刷新、下载记录与会话内本地路径无法安全打开、图片结果无法点击查看，以及项目刷新时计数短暂归零等问题。
- 修复快速连续切换任务时旧请求抢回当前会话、房间管家恢复耗时过长或未滚动到底部、活动过程不断累积展开，以及输入区出现多余焦点外框等交互问题。
- 工作区配置不再接受或覆盖已移除的 AI Provider 配置；ADO 端点变化仍会安全解绑旧 PAT。
- 删除仅覆盖旧管家运行时的过期回归用例，改由 Codex 原生工作区、宿主输入、审批、插件和已安排任务的真实 UI 用例覆盖。

### 发布

- 候选版本继续交付 Windows x64、macOS universal 与 Linux x64；公开发布仍需通过受保护工作流的构建、签名、产物、SHA256、Latest 与来源一致性校验。

## v0.36.5 - 2026-08-08

### 新增

- 管家工作台接入 Codex 原生 Skills 与 Plugin 市场：离线时保留内置和本地 Plugin，联网后恢复在线目录，并支持查看、复制定制与启停。
- 补齐 Codex app-server、Goal、共享 Agent 和 Azure DevOps 状态写入的真实生命周期与安全回归覆盖。
- 重做首次引导，用“可靠捕获—理清下一步—放进可信位置—按需出现”的流程说明 GTD 与注意力管理理念，再进入团队或个人配置。
- 管家新增持续维护的个人 Memory，把用户画像、偏好与工作习惯作为轻量上下文装载到后续任务中。

### 改进

- 管家纸面、完整对话、房间浮层和在办状态统一投影，动作审批、重试和结果不确定状态保持可见且可恢复。
- Windows 桌面端补齐业务 MCP、Codex 运行时和集成认证边界，减少跨进程恢复时的重复执行风险。
- 保留确定性的工作台界面，将 AI 查询、调整计划、安排任务和消息追溯统一交给 Skills 执行，避免前端重复实现业务工具。
- 委托与定时任务补齐直接触发入口，并把 Codex 运行中的用户输入与审批请求回传到任务卡片继续处理。
- “新建定时任务”在定时任务工作面直接打开创建表单，只展示无需补充房间等范围参数即可运行的已启用 Skill。

### 修复

- 中文姓名与拼音联系人搜索不再只覆盖首批 100 人；客户端会在安全上限内分页补齐并短期缓存目录，目录不完整时明确提示。
- 私聊会话头部头像同步显示在线、离开、忙碌或离线状态，多人直聊与群聊仍保持群组语义。
- 桌面开发环境固定使用独立的 IPv4 回环端口，并在端口冲突时直接失败，避免本机其他应用被误当成 RocketX 打开。

### 工程

- 新增 Codex 0.144.4 协议一致性、跨进程恢复、Skill 市场离线/在线切换及桌面回归门禁。
- 更新 PDF.js、Fastify 路由与构建链传递依赖，消除候选版本依赖审计中的已知高危漏洞。
- 管家禁用 Codex 原生 Memory 与旧版 `load_skill` 回退，长期记忆只经过 RocketX 可确认、可撤销、按账号和工作范围隔离的 Memory。
- Azure DevOps 使用 HTTP 传递 Windows 凭据或 PAT 时必须显式允许受信任内网认证，否则在凭据同步前直接拒绝。
- Azure DevOps 集成认证只允许访问工作台配置的 collection，并固定使用 Windows 系统 PowerShell，避免把凭据交给越界地址或 `PATH` 中的同名程序。
- 校准并验证管家工作面的 Linux Chromium 视觉基线，恢复候选分支的跨平台 UI 门禁。

### 发布

- 本版本候选目标继续恢复 Windows、macOS 和 Linux 三平台交付；正式公开前仍需通过受保护工作流的构建、签名、产物、SHA256 和 Latest 校验。

## v0.36.4 - 2026-07-30

### 修复

- 允许桌面端通过 Tauri `asset:` 协议读取已导入的个人贴纸，修复预览正常但发送时被 CSP 拦截并误报网络失败的问题。（#273）
- 贴纸卡片统一为纯图片布局，不再显示可能溢出的文件名式标题，同时保留悬停标题、无障碍标签和搜索能力。（#274）

### 发布

- 本版本继续通过受保护工作流交付 Windows x64、macOS universal 与 Linux x64 安装包、更新签名、插件包和 SHA256 校验文件。

## v0.36.3 - 2026-07-30

### 改进

- 本人头像卡片新增“编辑资料与状态”入口，可直接进入账号设置；切换在线、离开、忙碌或隐身后会同步服务器中的本人资料，并在导航头像和个人卡片中立即显示最新状态。（#271）

### 发布

- 本版本继续通过受保护工作流交付 Windows x64、macOS universal 与 Linux x64 安装包、更新签名、插件包和 SHA256 校验文件。

## v0.36.2 - 2026-07-30

### 修复

- 补充桌面端个人贴纸库的 Tauri 文件系统写入权限，修复导入图片、导入目录及收藏贴纸时因 `$APPDATA/sticker-library/**` 未纳入能力范围而报 `forbidden path` 的问题。（#269）

### 发布

- 本版本继续通过受保护工作流交付 Windows x64、macOS universal 与 Linux x64 安装包、更新签名、插件包和 SHA256 校验文件。

## v0.36.1 - 2026-07-30

### 修复

- 修复 Windows 桌面端停留在当前会话但窗口失焦时，新消息被立即标为已读、导致任务栏只闪烁却不显示未读数字角标的问题；重新聚焦窗口后才标记当前会话已读并清除角标。（#267）

### 发布

- 本版本继续通过受保护工作流交付 Windows x64、macOS universal 与 Linux x64 安装包、更新签名、插件包和 SHA256 校验文件。

## v0.36.0 - 2026-07-29

### 新增

- 设置中新增高性能模式；下次启动时会在内核初始化前关闭 Butler、Codex、共享 Agent、例行任务、OCR、运行时探测和相关轮询，隐藏 AI 专属入口并减少非必要动效，同时保留登录、会话、消息、搜索、上传和加载进度反馈。（#264）
- 主消息贴纸面板新增按服务器与账号隔离的本机个人贴纸库，支持导入图片、导入普通图片目录、导入 `qq-chat-exporter` 的 `pack_info.json + stickers/` 目录，以及从收到的图片消息收藏后继续复用现有上传确认流程。（#265）

### 工程

- 个人贴纸按 SHA-256 去重并限制条数与总字节；跨平台目录统一走 Tauri 路径 API，写入或元数据持久化失败时回滚本次新增文件。
- 首版个人贴纸库拒绝 SVG 与不明确的附件链接，不捆绑或重新分发第三方 QQ 贴纸资源。
- 高性能模式保留标准模式为默认值，并新增启动顺序、后台副作用、普通聊天流程和 UI 入口回归。

### 发布

- 本版本继续通过受保护工作流交付 Windows x64、macOS universal 与 Linux x64 安装包、更新签名、插件包和 SHA256 校验文件。

## v0.35.0 - 2026-07-29

### 新增

- 主消息输入区新增独立贴纸选择器，支持分组、搜索、最近使用、按需加载和 PNG/WebP/GIF 清单；首批内置 Twemoji 贴纸采用 CC BY 4.0，并继续复用现有上传确认流程。（#250）
- 「了解你」可在用户明确选择后读取当前连接和最近 14 天、最多 20 段 Codex 会话摘要，再由 Codex 生成带来源与证据的画像候选；候选仍需逐项确认，且不会自动写入长期记忆。Claude 暂无安全直读能力时会明确提示改用摘要导入。（#262）

### 修复

- 修复房间管家以 `📌` 开头的回答绕过 Markdown 渲染，导致粗体、正文链接和来源角标同时失效的问题。（#261）

### 工程

- 贴纸清单和资源固定为同源加载，损坏包、损坏条目和远程资源会被独立跳过；完整许可证随 Web 资源分发。
- 会话分类真实数据门禁兼容 Rocket.Chat 的 self-DM，同时继续验证普通单聊和多人直聊的成员边界。

### 发布

- 本版本继续通过受保护工作流交付 Windows x64、macOS universal 与 Linux x64 安装包、更新签名、插件包和 SHA256 校验文件。

## v0.34.5 - 2026-07-29

### 新增

- 「了解你」新增初始化入口，可把当前账号资料以及用户主动导入的 Codex、Claude、Azure DevOps 等背景资料整理为逐项确认、可撤销的画像候选。

### 改进

- 管家对话回归“先交流、再转为成果”：长回答始终保留完整正文，用户需要时再显式转入成果工作面继续。
- 房间管家支持拉到更宽并记住宽度；Markdown 段落与列表留出清晰间距，新输出在跟随状态下自动滚动到最新。
- 晨报里的工作项改为“编号、标题、真实链接”一起展示，并优先给出判断、处理顺序和风险，不再堆砌状态清单。
- 构建列表新增关联 PR 列、链接和按 PR 编号检索，同一 PR 的多次构建现在可以直接归组识别。

### 修复

- 修复一对一私聊仍显示无效提及入口、输入 `@` 打开成员补全并触发全局用户搜索的问题；群聊提及与邀请流程保持不变。
- 修复桌面管家调用 Azure DevOps Server CLI 时 PowerShell 运行时解析不稳、失败信息不清楚的问题。
- 修复 Azure DevOps PowerShell 回归测试在 Linux 和 macOS 上错误导入 Windows 专用辅助函数的问题。
- 移除管家头像选中后的重复勾选图标，继续使用边框和无障碍状态表达当前选择。

### 发布

- 恢复 Windows、macOS 和 Linux 三平台交付：由 GitHub Actions 服务器重新构建并发布 Windows x64、macOS universal 与 Linux x64 全套安装包、更新签名、插件包和 SHA256 校验文件。

## v0.34.2 - 2026-07-29

### 新增

- 新增「我的管家」专属档案页，可设置名称、头像、关系与相处方式，并集中查看透明档案、记忆和技能。
- 新增管家学习扩展：持续整理用户档案、分析工作节奏、发现重复操作，并把可复用的改进建议沉淀为小技能。
- 完整对话新增独立历史栏、自动标题和移动端切换器；房间问答按房间形成独立会话，AI 托管交流也能在同一历史栏只读回看。
- 内置技能改为卡片式管理，可查看说明、启用或停用；技能详情和例行照看继续使用同一套运行状态。

### 改进

- 统一管家各页面的字体层级、颜色、间距、卡片、按钮和深色主题，减少局部页面与 RocketX 主界面的视觉割裂。
- 房间引用改为正文角标与默认折叠的来源列表，每条事实分别对应自己的来源，不再把 1–8 个引用堆在回答末尾。
- 管家页面改用明确的人话动作词，移除「接住」等内部术语；导航、任务、例行照看、对话和管家档案的层级更清楚。

### 修复

- 修复进入房间管家或开启 AI 托管后，一级管家页面被错误切到上一个房间的问题；普通会话和房间会话现在互不覆盖。
- 修复 AI 托管记录未出现在管家对话历史的问题；记录直接读取 Rocket.Chat 原文，不复制、不产生第二份过期历史。
- 修复快速切换房间、账号或服务器时旧异步结果可能反写当前会话的问题，并允许托管历史恢复失败后再次尝试。
- 修复桌面窗口正文区域被误当成标题栏拖动的问题，以及多来源引用、会话预览和技能状态在真实交互中的若干显示错误。

### 工程

- 新增管家档案、学习扩展、工作区、技能管理、引用、会话隔离与 AI 托管投影的回归和 Playwright 覆盖。
- 管家运行时继续保持小内核：Rocket.Chat 是托管消息真源，`useButler` 负责管家会话状态，学习、档案和技能由扩展注册。
- 修复三平台收尾门禁把插件打包临时目录误判为 Release 资产的问题，同时继续拒绝任何未列入合同的多余文件。

### 发布

- 正式恢复 Windows、macOS 和 Linux 三平台交付：Windows 提供 NSIS slim、MSI 与 full 安装包，macOS 提供 universal DMG，Linux 提供 AppImage、DEB 与 RPM。
- 三个平台继续共用 Tauri updater 签名与完整 `latest.json`；Windows 自动更新固定选择 slim NSIS，full 只增加 OCR 资源且不包含 `codex.exe`。
- `v0.34.2` 恢复为 GitHub Latest，取代此前保留的 `v0.28.0` 跨平台更新基线。

## v0.34.1 - 2026-07-29

### 发布

- 此标签已由服务器完成三平台构建，但收尾门禁误把插件打包临时目录当成 Release 资产，因此从未公开；修复后的正式三平台交付由 `v0.34.2` 取代。

## v0.34.0 - 2026-07-29

### 发布

- 此标签对应已取消的 Windows-only 发布候选，从未公开；正式三平台交付由 `v0.34.2` 取代。

## v0.33.4 - 2026-07-28

### 修复

- 修复已存在兼容版管家数据库时 RocketX 启动即闪退的问题；升级后会保留原有待办和管家运行记录，不需要删除或重建数据库。
- 管家数据库仍拒绝未知的未来版本，避免用旧程序误写不兼容的数据结构。

### 工程

- 新增数据库版本 2 的启动与待办读写回归，以及版本 3 的拒绝回归。

### 发布

- 本次版本继续只发布 Windows x64 的 NSIS slim 与 full 两个安装包；full 只增加 OCR 资源，不包含 `codex.exe`。
- `v0.33.4` 不标记为 Latest；发布流程继续确认 `v0.28.0` 保持跨平台 Latest。

## v0.33.3 - 2026-07-27

### 改进

- 管家纸上的临时问答现在会在离开页面后保留，并按日期纸与 Butler 会话隔离；切到历史日期不会把今天的问答带过去。
- 临时问答失败会原位说明原因并提供小型“再试一次”按钮；重试不再算作新一轮，避免网络失败意外触发完整对话。
- 房间管家保持轻量浮层形态，支持鼠标与键盘调宽；连续多轮、关闭重开和进入完整对话都保留当前房间上下文。

### 修复

- 修复后续完整对话可能覆盖纸面原问题错误状态的问题；错误、重试和回答现在始终属于发起它的那一轮。
- 修复房间管家全屏时恢复流程把当前房间覆盖成普通管家入口的问题；完整对话页会明确显示当前工作面。
- 补齐例行事务开关和房间选择刷新持久化，以及 Desktop Todo 增删改查、逾期、补丁语义和旧 JSON 幂等迁移验证。

### 工程

- 发布门禁通过 220 条纯测试、763 条回归、67 条 UI、60 条桌面 Rust、13 条局域网插件 Rust 与 53 条真实 Rocket.Chat smoke。
- Butler 动态工具、原生 Skill、Azure DevOps Server 托管适配器和 Codex app-server 真实 turn 探针全部通过。

### 发布

- 本次版本继续只发布 Windows x64 的 NSIS slim 与 full 两个安装包；full 只增加 OCR 资源，不包含 `codex.exe`。
- `v0.33.3` 不标记为 Latest；发布流程继续确认 `v0.28.0` 保持跨平台 Latest。

## v0.33.2 - 2026-07-27

### 修复

- 修复房间管家从浮层全屏进入完整对话时丢失当前房间上下文的问题；现在会先把当前房间写回管家上下文，再打开同一段完整对话。
- 补充房间管家全屏上下文回归，确保未先发言时也能直接进入带房间来源的完整对话页。

### 发布

- 本次版本继续采用 Windows-only 发布模式（NSIS 双包：slim + full），继续维持 `v0.28.0` 作为跨平台 Latest 标识。

## v0.33.1 - 2026-07-27

### 改进

- 管家完整页和房间浮层重构：保留“今日纸面”单页结构，加入“消息与提醒”版块，优化到办事项折叠/快速处理流程，并明确把临时问答作为会话层内可回看内容。
- 房间浮层改为“当前房间源化”展示，不再混入其他房间内容；全屏进入完整对话后仍保持同一会话状态，返回纸面可继续读写上下文。
- 新增提醒与消息事件回流入口（@我提醒）在纸面可直达对应房间，失败时可一键重试并保留已有结果。

### 修复

- 修复历史上“未显示待办导致纸面误判为空”的错误：当有未完成待办时，纸面不再被错误提示为空。
- 修复管家完整页/房间浮层“一直 loading”卡住状态：运行时/错误/空态分离展示，运行失败不阻塞上下文渲染。
- 修复会话切换时的历史丢失与混淆：对会话管理、房间来源过滤、完整对话与任务区共享状态做了回归收敛。

### 发布

- 本次版本继续采用 Windows-only 发布模式（NSIS 双包：slim + full），继续维持 `v0.28.0` 作为跨平台 Latest 标识。

## v0.33.0 - 2026-07-27

### 改进

- 管家完整页收口为一张“今天的纸”：`等你点头 / 在办 / 今天` 继续做主线，完整对话与管家管理改为同页折叠展开，收起时不重置会话或输入草稿。
- 房间里的管家改为消息区右下角唤起的浮层，只保留当前房间相关的在办事项和最近一次问答，不再长期挤占会话布局。
- 完整对话改为左右分居的双侧气泡，今天事项与在办默认收起，首屏先让用户看清楚今天真正要处理的事。
- Windows 安装包改为依赖系统 Codex；默认安装不再为可选重资源付费，full 安装包也不再额外捆绑 `codex.exe`。

### 工程

- 新增管家日期纸、房间浮层、折叠运行项、会话历史快照、房间来源过滤和能力库装载语义回归，继续把管家状态投影收敛到同一套运行时数据上。
- 发布校验放宽为“标签提交在 `main` 历史上即可公开”，不再要求发布时标签必须等于最新 `main` 头提交。

### 发布

- 桌面 Release 只提供 Windows x64 的瘦版 NSIS、full NSIS 两个安装包，以及 Updater 元数据、签名、插件包和 SHA256 校验；本版不发布 macOS 或 Linux 安装包。
- `v0.33.0` 公开后仍不标记为 Latest，发布工作流会重新确认 `v0.28.0` 继续作为跨平台 Latest。

## v0.32.0 - 2026-07-26

### 改进

- 「AI 提取为待办 / 工作项」改由 Codex 完成，装好客户端就能用，不再需要单独配置 AI 服务密钥；提取过程中会显示进度，完成或失败都有明确回应（#229）。
- 界面上 12px 的小字收回到角标、徽章和键帽，成句的文字统一提到 13px 起步，层次改由颜色区分——1080p 上的中文小字更清楚。
- 管家里的「转到 Codex」改名为「在 Codex App 打开」，并说明需要你按回车发出：它只把内容填进 Codex App，不会自动开跑。想让任务自己跑完，用管家的「派活」（#201）。

### 修复

- 修复使用 OCR 后内存占用持续升高且不再回落的问题：现在会挡住超大图片、把过大的图缩小后再识别、调低识别批大小，并在空闲五分钟后释放识别引擎（#235）。
- 修复别人在话题里回复并勾选「同时发送到频道」时，那条消息在主消息流里不显示的问题——此前只能在「提及我的」里看到它（#232）。
- 修复从官方 Rocket.Chat 客户端收到「图片 + 文字」时只显示图片、说明文字丢失的问题（#189）。
- 修复群里有讨论组时未读消息一直清不掉的问题：打开群后紧接着点进讨论，会让群的已读请求被取消且不再补发；另外托盘和任务栏此前会为已隐藏的会话一直亮着红标，而那个会话在列表里根本找不到（#134）。
- 修复自动留存的附件被反复重新下载的问题：因容量上限或留存期到期而清理掉的文件，现在不会再被自动下载回来，需要时仍可手动获取（#217）。
- 修复 AI 相关请求失败时被误报成「没有权限执行此操作」的问题——那是 Rocket.Chat 的权限提示，用在这里会把排查方向带偏（#229）。

### 工程

- 回归从 709 条增加到 732 条，新增未读口径、附件清理墓碑、字号档位、话题回显和附件说明渲染等回归。
- 未读提醒的计算口径收敛到一处，此前标题栏、任务栏、托盘、侧边栏和工作台各算各的。
- 修复本地运行全量回归时两条与改动无关的失败（Git Bash 的 tar 无法处理盘符路径），以及界面文案调整后未同步的 UI 冒烟断言。

### 发布

- 桌面 Release 继续只提供 Windows x64 的 MSI、NSIS、Updater 元数据、签名、插件包和 SHA256 校验；本版不发布 macOS 或 Linux 安装包。
- `v0.32.0` 公开后仍不标记为 Latest，发布工作流会重新确认 `v0.28.0` 继续作为跨平台 Latest。

## v0.31.1 - 2026-07-24

### 修复

- 管家完整页和房间侧栏现在都支持直接粘贴剪贴板图片，并继续复用已有的格式、数量、大小校验与预览发送链路（#225）。
- API 管家现在能从技能索引加载 `azure-devops-server`，并按 RocketX 托管的只读 CLI 契约查询 Azure DevOps Server，不再错误报告只有晨报、晚间回顾和周报技能可用（#226）。

### 工程

- 新增完整管家页、房间侧栏的真实浏览器图片粘贴回归，以及 API 管家托管 Skill 索引和加载契约回归。

### 发布

- 桌面 Release 继续只提供 Windows x64 的 MSI、NSIS、Updater 元数据、签名、插件包和 SHA256 校验；本版不发布 macOS 或 Linux 安装包。
- `v0.31.1` 公开后仍不标记为 Latest，发布工作流会重新确认 `v0.28.0` 继续作为跨平台 Latest。

## v0.31.0 - 2026-07-24

### 新增

- RocketX 现在把 Azure DevOps Server 能力作为标准 Codex Skill 随桌面端交付，并通过单一受控的只读宿主适配器复用仓库、工作项、构建、测试、发布、Wiki 与搜索能力；原有 `ado-bridge` 收敛为 Service Hooks 通知入口。

### 改进

- “我的工作项”默认隐藏明确处于搁置状态的条目，并记住用户选择的筛选；即使当前结果暂时没有已保存的精确状态，也不会静默回退（#213）。
- 聊天消息支持安全渲染行内和块级公式，同时保留现有代码、链接、表情与可访问 MathML 输出（#218）。

### 修复

- 修复 Rocket.Chat 过期附件清理更新被误判为新消息并触发提醒的问题（#212）。
- 修复手动清除某房间的本地附件后，历史刷新或重连又自动下载相同文件的问题（#217）。
- 修复设置群主、管理员或负责人时，把执行者姓名误显示成角色名称的问题（#220）。
- 修复 Windows-only Release 公开后 GitHub Latest 可能漂移到不完整跨平台更新清单的问题。

### 工程

- 新增 Azure DevOps Server Skill/宿主边界、工作项筛选、公式渲染、附件清理、角色消息和发布 Latest 策略回归，并稳定 UI 异步断言时序。

### 发布

- 桌面 Release 继续只提供 Windows x64 的 MSI、NSIS、Updater 元数据、签名、插件包和 SHA256 校验；本版不发布 macOS 或 Linux 安装包。
- `v0.31.0` 公开后仍不标记为 Latest，发布工作流会重新确认 `v0.28.0` 继续作为跨平台 Latest。

## v0.30.0 - 2026-07-23

### 新增

- 管家完整页和房间侧栏现在都能选择、预览、移除并发送图片；API Provider 与 Codex 会收到真实多模态图片输入，而不是只看到附件占位文本（#198）。
- 通知聚合改为默认关闭的“禅模式”，用户可以在设置中主动开启；已有明确启用配置继续保留（#208）。

### 改进

- 下载历史现在保留原始服务器、房间和消息来源，用户可以从下载页追溯文件来自哪段会话（#191）。
- 管家遇到界面语言偶发不合规时会自动重试当前回合，不再让整轮任务直接失败（#184）。

### 修复

- 修复桌面端已经下载成功的文件可能被打开权限规则拒绝的问题（#183）。
- 修复取消文件拖拽后遮罩仍永久停留、阻挡后续操作的问题（#194）。
- 修复给窗口底部消息添加表情时，表情选择面板被视口下边缘截短的问题（#207）。

### 工程

- 新增管家双入口图片交互、多 Provider 图片映射、下载来源、拖拽取消、语言重试、禅模式持久化与底部表情面板的自动化回归覆盖。

### 发布

- 桌面 Release 继续只提供 Windows x64 的 MSI、NSIS、Updater 元数据、签名、插件包和 SHA256 校验；本版不发布 macOS 或 Linux 安装包。
- `v0.30.0` 公开后仍不标记为 Latest，`v0.28.0` 继续作为跨平台 Latest，避免现有 macOS/Linux 客户端读取不完整的更新清单。

## v0.29.1 - 2026-07-23

### 新增

- 管家为“找文件、比较 PR、提取承诺、逾期工作项跟进草稿、构建失败关联、周报例行任务、跨重启续跑”七类任务建立结构化 task state；每项任务明确记录能力预检、来源新鲜度、禁止动作和恢复合同（#168、#173）。
- API 与 Codex 现在共享同一套管家 session/engine 生命周期；切换运行模式时会桥接尚未看到的对话，恢复失败时也会把可见 transcript 交给新线程继续（#174）。
- 管家长期记忆收敛为带 `server/account[/project/room]` 作用域的 alias、preference 与 commitment；支持显式召回、撤销、恢复和经批准的旧记忆导入（#176）。
- Today、主动简报、@我 watcher 和例行事务接入同一 workflow runtime，共享任务状态、来源、审批、checkpoint、暂停、失败重试和跨重启恢复（#177）。

### 改进

- “比较这两个 PR”等指代不完整的请求会在进入 API 或 Codex 大脑前只询问缺失的最少信息；用户补充后沿用同一任务，不再猜测对象。
- 工具返回的消息、工作项、PR 和构建来源会同步写入当前任务；完成、停止和失败分别保留可恢复状态，并随独立管家会话跨重启恢复（#173）。
- 所有管家写工具统一执行 typed 参数校验、能力预检和可见审批；首次调用只生成 checkpoint，明确确认后才执行，失败不会被误记为成功（#175）。
- 主动简报最多保留三条值得立即处理的事项，并同时说明原因与建议动作；计划任务在登录前不会误记为已经执行，暂停 Codex 主动任务会真实中断当前回合（#177）。

### 安全与隐私

- 长期记忆不再隐式注入系统提示；召回改为显式工具，写入、撤销、恢复和旧数据导入均需要审批，遗留 v1 记忆默认隔离（#176）。
- 写操作审计只记录 operation、工具、能力、幂等键和状态，不记录参数正文或预览内容；API 与 Codex 不能绕过同一审批边界（#175）。

### 工程

- 保留现有 Provider 与 Codex adapter、Today 结果和 routine/event card 读模型，不新增第二套导航或状态机；主动 workflow session 对人工会话选择器保持隐藏（#174、#177）。
- 新增任务编译、双运行模式切换、typed tool runtime、作用域记忆、主动 workflow、跨账号并发、审批投影与真实 Codex 中断回归（#173–#177）。

### 发布

- 桌面 Release 在当前稳定化阶段仅提供 Windows x64 的 MSI 与 NSIS 安装包、对应 Updater 元数据和签名；macOS 与 Linux 官方安装包延后到平台验证稳定后恢复。
- Windows-only Release 采用非 Latest 发布策略，Windows 用户在公开后从 Release 页面手动下载；`v0.28.0` 暂时保留为跨平台 Latest，避免现有 macOS/Linux 客户端读取缺少平台条目的更新清单。

## v0.29.0 - 2026-07-23

### 发布

- 此受保护标签完成过三平台草稿构建，但在公开前因发布范围收敛而中止；标签保留且没有公开 Release，正式交付顺延为 Windows-only 的 v0.29.1。

## v0.28.0 - 2026-07-23

### 新增

- 管家现在支持在同一服务器账号下创建、切换和重命名多个独立会话；每个会话分别保存 transcript、模型历史、最近活动时间与 Codex 恢复点，并可在重启后继续（#172）。

### 改进

- 移除管家固定三天未活动就截断上下文的 TTL；历史会话不再因时间经过而退化为只读回看。
- 管家完整页与房间 AI 侧栏共用同一个会话选择器，当前活动会话在两个入口保持一致。

### 工程

- 旧版 `builtin:butler` 单会话记录会无损迁入默认会话，并继续镜像当前活动会话供旧版本回滚读取；新增服务器、账号、并发 hydrate 和 API/Codex 在途回合隔离回归（#172）。

## v0.27.0 - 2026-07-22

### 新增

- 桌面端可按需自动留存已查看消息中的附件到本机：功能默认关闭，可配置单文件上限、账号总配额和保留天数；设置页按房间展示占用并只删除本地副本，不改动 Rocket.Chat 服务器原文件（#152）。
- 下载页新增成功下载历史，可直接打开文件或定位所在目录；清除历史只移除本地记录，不删除已下载文件（#169）。

### 改进

- 图片文字识别默认改用随安装包提供的 PP-OCRv5 与 ONNX Runtime，Windows、macOS 和 Linux 均可完全离线识别；Windows 系统 OCR 仅作为本地降级路径，图片不会静默上传到云端（#163）。
- 图片预览继续支持在原图上拖选和复制识别文字，并明确显示实际使用的本地识别后端。

### 工程

- 为管家 M12 重构建立可执行的 P0 架构基线、任务证据回归和 P1–P6 子 Issue；本版不把后续会话、上下文、引擎、工具、记忆与主动轮次实现误报为已完成（#168、#172–#177）。
- 新增附件留存、下载历史、中文 OCR 真实夹具、离线缓存、三平台资源打包和完整桌面交互回归。

## v0.26.1 - 2026-07-22

### 修复

- 修复桌面端下载大文件时界面长时间无响应的问题：附件现在从 Rocket.Chat 响应流直接分块写入本地文件，不再在 WebView 中完整缓冲后通过单次 IPC 传输（#164）。

### 工程

- 保留原有文件认证、同源跳转和跨源凭据剥离规则，并新增未缓冲下载响应回归。

## v0.26.0 - 2026-07-22

### 新增

- Windows 桌面端图片预览新增完全本地的系统 OCR：识别结果直接覆盖在原图上，可像普通文字一样选择，并可一键复制全文；图片不会发送到云端，也不额外捆绑模型（#153）。
- 团队工作区配置可直接内联 Azure DevOps 工作项模板，同时继续兼容原有模板 URL，团队可以用单个 `rcx.workspace.json` 完成首次配置（#154）。

### 修复

- 修复非宿主打开含历史 `@ai` 消息的会话时，旧指令被再次执行并误报“当前 Agent 会话仅宿主可指挥”的问题；实时新增、编辑和发送确认仍按原规则处理（#158）。

### 工程

- 新增 Windows 系统 OCR 真实图片夹具、坐标归一化与文本间距回归、浏览器文字选择交互，以及首次历史加载不重放 Agent 指令的回归覆盖。

## v0.25.16 - 2026-07-22

### 修复

- 修复输入文字后选择、粘贴或拖入图片/文件时只发送附件、丢失文字说明的问题；正文现在与首个文件作为同一条消息发送，多文件不会重复正文，失败时保留输入（#155）。

### 工程

- 新增上传正文单元回归、Playwright 图文确认交互和真实 Rocket.Chat `rooms.mediaConfirm` 冒烟验证。

## v0.25.15 - 2026-07-22

### 工程

- npm 发布重跑时会在跳过已发布 SDK 前重新生成构建产物，确保全新 runner 仍能继续编译并发布 CLI。
- npm 包发布后最多等待 60 秒，直到版本与 `gitHead` 都能从 Registry 查询到，避免传播延迟把已成功的发布误报为失败。

## v0.25.14 - 2026-07-22

### 变更

- 桌面端首次启动改为团队优先引导：先说明 RocketX 的协作理念，再从团队提供的配置 URL 或本地文件一次设置 Rocket.Chat、Azure DevOps、AI、模板与更新源；个人模式仍可直接使用。
- 团队配置在应用运行期间每 24 小时自动检查一次，有变化时只提醒并展示逐字段预览，不会静默覆盖成员自己的配置。

### 修复

- 修复桌面端重启后团队配置 URL 未重新授权、同步失败无提示、跟随设置丢失，以及本地文件导入后仍保留旧 URL 的问题。
- 修复团队端点变化时继续沿用旧凭据或会话的问题：ADO 地址变化会解绑 PAT，AI 端点变化会解绑密钥，Rocket.Chat 地址变化会清理旧会话并要求重新登录。
- 内网 HTTP 与共享目录更新统一使用发布签名校验；未签名、签名无效或不在配置目录中的安装包将被拒绝执行。

### 工程

- 新增首次引导、重启同步、端点凭据隔离、Rocket.Chat 会话切换和共享目录签名更新回归；团队配置文档明确当前 Git 支持的是无需登录的 Raw URL，私有仓库可使用受控 HTTP 或本地文件。

## v0.25.13 - 2026-07-21

### 变更

- RocketX 应用 SDK 采用项目可控的公开 npm 身份 `@lusipad/rocketx`；`create-rcx-app` 保持原包名，并自动依赖同版本 SDK。

### 工程

- 发布合同、脚手架导入、开发文档和 clean-room 打包测试统一迁移到新 SDK 身份；npm 可信发布仍独立于桌面/GitHub Release，不向 RocketX 核心引入包管理器逻辑。

## v0.25.12 - 2026-07-21

### 修复

- 修复 Windows 启用内网通官方插件时额外弹出命令行窗口的问题；Native Sidecar 现在使用无控制台窗口方式启动（#144）。

### 工程

- 新增 Windows 进程级回归，由 Sidecar 子进程直接核验 `GetConsoleWindow()` 返回空；协议门禁归一化 Windows CRLF 与生成器 LF，避免无语义漂移误报；GitHub 桌面 Release 与可选的 npm 包发布解耦，补丁版不再被无关 Registry 状态阻塞。

## v0.25.11 - 2026-07-21

### 变更

- 飞鸽 / IPMSG 相关功能完整迁入官方插件的独立 Rust Sidecar；RocketX 核心只保留通用、受限的签名 Native Plugin Runtime，不再包含旧协议命令、2425 socket、GBK 编码、联系人 Store 或兼容频道。
- 标准 IPMSG/飞鸽继续支持发现、消息和普通文件；新增真实 `1@shiyeline` 2425 fixture，原版内网通收缩为发现和文本，不再监听或宣称兼容私有 `9011` 文件协议。
- 过滤侧车在多网卡环境收到的自身广播，设备列表不再把本机重复显示为联系人；旧版内置插件升级时自动迁移到新的 Sidecar 权限并保留启停状态。

### 工程

- Windows 桌面包构建并映射签名 Sidecar 到受限插件资源目录；CI、插件源码包、真实 iframe Bridge 和进程级 2425 启停测试覆盖新的通用 Native Service 边界。

## v0.25.10 - 2026-07-21

### 修复

- 修复桌面发布版的安全策略阻止内置应用 Bridge 与插件脚本执行，导致内网通名称无法读取或保存、发现范围无法加载、设备列表始终停在“尚未读取”的问题。发布版现在可即时应用名称并在重启后保留。

### 工程

- 新增直接读取桌面端生产 CSP 的真实 iframe 交互回归，覆盖名称读取与保存、发现范围应用和设备刷新；发布前使用 Windows release 构建验证名称持久化、设备重新广播及 UDP 9011/2425 监听。

## v0.25.9 - 2026-07-21

### 新增

- 管家会携带当前房间、待办、日程或 ADO 工作面的上下文回答，并在回答下展示可返回原位置的来源；回答可以先生成可编辑的回复、待办、承诺、ADO 工作项或 Codex 交接草案，只有用户确认后才执行，并在工作日志留下提议、取消、成功或失败记录。
- 内网通作为 RocketX 内置应用随桌面端提供，首次仍默认关闭；可设置独立的本机显示名称，保存后立即重新宣告设备，升级时保留已有开关状态。

### 修复

- 修复“转到 Codex”只生成列表可见、实际无法继续执行的孤儿线程；管家和 AI 托管现在通过 Codex App 官方新对话链接带入工作区与完整记录，由用户确认后发送。记录超过系统 URL 上限时会完整复制并打开对应工作区，不会静默截断（#105）。
- 修复内置应用重新加载后 Bridge 不再连接，导致设置无法保存、设备无法发现的问题；同时修正内网通联系人下拉框在深色主题下文字与背景对比不足。

### 工程

- 新增管家来源跳转、动作确认/取消、回复不自动发送、待办与承诺落盘、ADO 表单以及跨工作面共享会话的浏览器交互测试；补充内置应用默认关闭、升级保留状态、禁止卸载和 Bridge 重连回归测试。
- 修复桌面端外链校验测试写死 Windows 路径、导致 macOS 与 Linux 标签构建在 Rust 单元测试阶段失败的问题；测试现在使用运行器自身的临时绝对路径。

## v0.25.8 - 2026-07-21

### 工程

- 此标签的 macOS 与 Linux 桌面构建因跨平台测试夹具错误失败，未生成或公开 Release；修复后顺延为 v0.25.9。

## v0.25.7 - 2026-07-20

### 新增

- 内网通官方插件新增跨网段发现范围配置，支持单个 IPv4、CIDR 和起止范围，合计最多 1024 个目标；同一配置同时向内网通 `9011` 与 IP Messenger `2425` 宣告，留空时继续使用自动广播。

### 工程

- 发现范围在 Rust 宿主内校验并随账号分区持久化，启用中修改会重新启动插件运行时；无效、反向、组播或过大范围会在发包前被拒绝，插件仍默认关闭并可随时禁用或卸载。

## v0.25.6 - 2026-07-20

### 修复

- 修复右键回复在 RocketX 本地看似带引用、但 Rocket.Chat 官方客户端只收到普通消息的问题；界面发送和真实服务冒烟现在复用同一引用格式（#126）。
- 内网通在多网卡 Windows 设备上按各网卡掩码发送定向广播，并每 30 秒重新宣告，避免插件已启用却长期看不到设备；插件仍默认关闭，禁用或卸载后完整停止（#127）。
- 外部 HTTP(S) 链接改由经过协议校验的桌面原生命令交给系统浏览器，失败时给出明确提示；Codex 线程深链也走同一条受限原生路径，失败时继续复制 `codex resume` 兜底（#129、#132）。
- 引用消息携带其它话题 ID 时，优先回到当前房间已存在的 AI 托管会话；中断后恢复不再静默忽略字面量 `@ai`（#130）。
- AI 托管会把 Rocket.Chat 图片下载到会话隔离的临时目录，并作为 Codex `localImage` 输入提交，不再只有附件文件名而无法识图（#133）。
- 管家工具可搜索并读出只有标题、没有来源消息的待办，避免提议入账后再次显示“（无文字内容）”（#119）。

### 工程

- 每个 PR 新增 Windows 桌面端 Rust 门禁；引用回复增加真实右键 UI 路径，外链权限增加防回退检查。本地使用官方签名的 IP Messenger 5.8.3 验证了双向发现、消息确认和文件传输。

## v0.25.5 - 2026-07-20

### 新增

- 内网通作为 RocketX 官方插件随发布包提供，兼容 9011/GBK 与 IP Messenger 2425 协议；插件首次安装默认关闭，启用时才启动监听，禁用或卸载后完整释放端口并清理插件状态。

### 修复

- 修复管家提议入账后，只有标题的待办显示“（无文字内容）”的问题（#119）。
- “转到 Codex”现在创建原生线程后直接打开对应 Codex App 会话；AI 托管面板明确提供“转到 Codex App”入口，打开失败时复制 `codex resume` 命令（#120）。
- 工作台层级工作项按 Epic 橙色、Feature 紫色、User Story/需求蓝色、Task 绿色区分，并保留现有折叠与筛选能力（#121）。
- 修复偏好尚未加载时临时变成 Ctrl+Enter 发送的问题，首次打开即按默认 Enter 发送（#122）。
- 删除消息时始终使用消息自身的房间 ID，请求服务器按原发送者删除并等待成功确认后才更新本地列表（#123）。
- 官方插件身份改由宿主校验内容指纹，同 ID 的第三方包无法冒充内网通插件访问旧协议能力。

## v0.25.4 - 2026-07-20

### 新增

- AI 管家与 AI 托管现在可分别配置 Codex 模型和推理强度：新安装默认使用管家 `medium`、托管 `high`；升级时托管配置只会首次继承既有管家设置，之后两者互不影响。

### 修复

- 修复收起分组栏后底部出现水平滚动条的问题（#114）。
- 修复切换并打开聊天时偶尔沿用上一会话滚动位置、没有定位到最新消息的问题（#115）。
- 修复聊天栏收窄后 Azure DevOps 工作项、PR 与构建卡片被裁切的问题（#116）。
- 待办说明和来源消息中的网址现在可以直接点击打开（#117）。

## v0.25.3 - 2026-07-20

### 新增

- 「转到 Codex」改用与 Codex Companion 相同的原生线程机制，管家对话和群托管对话会直接出现在 Codex App / CLI 会话列表中，并保留完整上下文继续处理。
- 更新源支持 GitHub、内网 HTTP 和共享目录三种模式；设置页可随时检查更新，受限网络环境也能接收版本提醒并打开或启动对应安装包。
- 层级工作项支持 Epic / Feature / 故事三档起点与「拆分开发、测试 Task」开关组合成六种形态，创建前可预览结构并记住默认选择。
- 团队工作区配置支持订阅更新，每天检查服务器地址、更新源和工作项层级默认值的变化，先展示差异再由用户确认应用，不会静默覆盖本地配置。
- 提供可直接复制修改的团队工作区与工作项模板样例，并用回归测试保证样例持续符合当前配置格式。

### 修复

- 修复「转到 Codex」因外部会话导入路径不被识别而失败的问题，并将生成过程显示在所属回答旁，工作项与 PR 编号保持可点击。

## v0.25.2 - 2026-07-19

### 新增

- 管家 v3「对账员」：大脑巡视替代规则警报——拿当前世界状态对承诺台账，差异才开口；简报每轮至多 3 条，每条必带「为什么找你」与建议动作。
- 台账与提议制：「我答应的 / 我在等的」两列台账；新指派、聊天里许下的承诺以提议卡呈现（带房间名与原话），入账/销账一键落库，「先不管」后同一事项永不再提。
- 工作日志：每轮巡视「看了 N 项、压下 M 条」全量可审计，被压条目附理由——管家的沉默有账可查。
- 「少来这种」：静音即时生效且由代码强制（命中项降入日志），静音列表可查看可删除。
- 巡视触发：每日首开、手动「再看一圈」、离开 2 小时后回来、18 点晚间收尾（当天唯一），以及新指派/构建转红时的提前巡视（只触发巡视，不弹通知）。
- 简报一键动作：转任务（防重）、稍后（仅当轮生效）、帮我拟一句（草稿仅可复制或跳会话，绝不代发）。

### 变更

- 三面合一：「今日」与「AI」入口并入唯一「管家」入口；对话在桌面停靠展开，历史完整延续；例行事务管理与投递迁入桌面；旧入口的持久化状态自动迁移。
- 构建状态按流水线归并只看最新一次，工作台队列与相关视图同步生效——被后续成功覆盖的失败不再出现。

### 移除

- 六规则警报引擎与三级提醒（v0.24.6 引入）整体退役，仅保留「承诺到期」的系统通知安全网；仅改标签的工作项变更、已被修复的构建失败不再产生任何提醒。
- 咖啡时间调度器与警报表达层随之移除（表达并入巡视人设）。

## v0.24.7 - 2026-07-19

### 修复

- Butler 按持久化轮询水位识别新的 P1/P2 工作项，避免历史 P1 误报，并让 P2 正确进入咖啡时间。
- 迭代压力仅在唯一活跃 ADO 项目可确定时计算，修正剩余两天和 P2 提醒的人格化文案。
- 接通“我答应给谁 / 我在等谁”的待办创建、编辑与 SQLite 持久化链路，旧双值数据保存后归一化为单一方向。
- 稳定 Butler 主导航、错过咖啡时间、头像代理与会话初态相关的 UI 和实时 smoke 测试。

## v0.24.6 - 2026-07-19

### 新增

- 管家系统（Butler）：六规则引擎 + 三级提醒（即时/咖啡时间/静默），支持承诺到期、逾期、构建失败、待办逾期、@提及超时、新分配任务检测。
- 管家表达层：四维人格轴（详略/深浅/语气/紧迫度）影响提醒标题生成，结构化 AlertContext 避免解析中文标题。
- 咖啡时间调度器：支持多时间点（默认 09:00/19:00），错过的时段打开后自动补触发。
- 管家轮询桥组件（ButlerPollerBridge）：generation token 取消过期轮询，请求序号丢弃陈旧 UI 更新。

### 变更

- 今日页管家视图新增为独立模块（butler-view），位于 today 与 todos 之间。

## v0.23.0 - 2026-07-18

### 新增

- AI 对话本地持久化：对话与模型历史按 服务器+账号 保存，重启后恢复；Codex 大脑经 thread/resume 接续原线程上下文。
- 上下文防腐：「新对话」按钮（AI 页面 + 房间 AI 面板）清空对话并丢弃 Codex 常驻线程；超过 3 天没对话时恢复仅供回看、模型上下文自动从头开始。
- AI 回答可停止：运行中发送按钮变为停止，已生成内容保留、不当错误处理；执行过程可见——每次工具调用记录为步骤（进行中/成功/失败），运行时实时展开，结束后收起可回看。
- 「转到 Codex」：AI 页面对话与群托管对话都可经官方外部 Agent 会话导入器转移进 Codex，在 Codex App / CLI 的会话列表里可见并可继续（app-server 直接创建的线程属于自动化来源，App 列表默认不显示）。托管消息带说话人前缀，导入产物是快照副本。
- 搜索框新增「问 AI」：把输入交给 AI 大脑，用大模型理解语义并调用搜索工具回答。
- AI 设置新增「人设」编辑：自定义聊天 AI（AI 页面、房间面板与晨报）的性格与回答风格，可一键恢复默认；AI 托管的编码代理与安全纪律不受人设影响。

### 变更

- 今日页重设计：AI 区改为「AI 晨报」只放 AI 生成的报告，例行事务管理控件收进折叠区；构建失败/@我超时/新分配等规则提醒摘掉 AI 标签，移到独立「提醒」组（可跳转、可关闭、不计入处理进度）。
- 整体移除 embedding 向量语义搜索（#95）：能力路由不再有「语义搜索」，Provider 配置去掉 embedding 模型字段，搜索框的语义模式删除；「语义搜索」由「问 AI」承担。旧配置里的残留路由键会被自动忽略。

## v0.22.0 - 2026-07-18

### 修复

- 全局快捷键选中联系人后，光标直接进入消息输入框；弹窗关闭时不再从已聚焦的输入框抢走焦点（#87）。
- AI 助手删除本地正则命令解析：所有输入（含快捷提示）一律交给 AI 大脑调用工具查证后回答，「再帮我看看还有哪些需要我处理的 PR」这类问法不再返回「没有符合条件的记录」（#89）。
- 打开 AI 页面默认停在最新对话；执行间消息流与托管面板「本地过程」同样贴底跟随，滚上去阅读历史时不打扰（#90）。
- 回复时发送图片/文件（含桌面端拖拽本地文件）不再丢失引用缩进；发送确认弹窗会标明「将作为回复发送」。挂着引用时不走局域网直传，保证引用完整（#91）。
- 复制图片消息写入真实位图，粘贴到微信、画图等应用得到的是图片而不是 `![image.png](/file-upload/…)` 链接文本；环境不支持时回退文本复制（#92）。
- 新版 Codex CLI 下 AI 大脑与 AI 托管不再报「Codex app-server 已退出（2）」：`app-server --stdio` 与 `codex exec` 的可选参数按当前 CLI 的 `--help` 探测后再传，跨版本可用；`--sandbox read-only` 等安全参数不降级。

### 变更

- AI 设置重新分组：「AI 运行方式」提为页面顶部的基础设置；高级设置按保存方式分为「模型 Provider / 按能力路由」（由保存按钮统一保存，按钮标明作用范围）与「外部集成」（反向 MCP、共享 Agent 身份，各自即时生效）。
- RocketX 创建的 Codex 线程统一命名——「RocketX 托管 · 工作项/房间名」「RocketX 执行间 · 目录名」「RocketX AI 大脑」，在 `codex resume` 与 Codex App 的会话列表里可辨认；托管面板新增一键复制 `codex resume <线程>` 入口。

## v0.21.1 - 2026-07-18

### 修复与变更

- 修复桌面端误报「无法读取 Codex CLI 版本」（#86）：
  - Windows 上解析出的 codex.cmd / codex.exe 路径不再携带 `\\?\` 扩展前缀——该前缀会让 Node 无法加载 npm 包装的 codex.js 入口，导致 `codex --version` 非零退出；app-server 的工作目录同样改用常规主机路径。
  - 版本探测放宽解析：兼容包装脚本追加的提示行、打到 stderr 的版本信息与 `codex 0.x.y` / `v0.x.y` 等格式；退出码非零但输出里有明确的 `codex-cli` 版本行时不再直接判失败，可用性仍由 app-server 与登录探测把关。
  - 读取失败时错误信息带上退出码与 stdout / stderr 摘要，便于定位环境问题。

## v0.21.0 - 2026-07-18

### 新增

- 工作台自定义查询结果支持「列表 / 看板 / WBS」三种视图，按查询记忆选择。数据源就是 ADO 上已维护的查询：查询定义看什么，视图只负责怎么看（#82、#83）。
  - 看板：列 = 结果里真实出现的状态，按 新建→进行中→已解决→完成 归类排序，中英文状态都认；逾期卡片排最前。卡片可拖到别的列改状态，确认后写入 ADO，是否合法流转由服务端裁决。
  - WBS：按父子关系组树，父节点显示子树进度（完成数/总数），并标注规则化风险信号——逾期、停滞（超 7 天无更新）、未指派；顶部有整体进度与风险汇总。
- 设置页新增「工作区」分区：从 URL（含 Git raw 地址）或本地文件导入 rcx.workspace.json，一次配好 Rocket.Chat 地址、ADO 连接、AI Provider 和工作项模板地址（#67）。配置提供默认值：本地空值默认写入、与配置一致跳过、用户自己改过的字段默认保留（可勾选强制覆盖）。
- 待办支持手动新建，不再要求从聊天消息标记；手动待办以「要做什么」为正文，列表、快搜、AI 助手、今日、工作台等入口同步适配（#64）。

### 修复与变更

- 讨论加入在 `rooms.join` 返回 405（部分反代/网关对不支持端点的响应）时也回退 DDP `joinRoom`，不再把 405 直接抛给用户（#62）。
- 修复中文输入法激活时应用内快捷键失效：IME 把 Ctrl+Shift+F 这类组合键报成 `e.key='Process'`，现按物理键位兜底识别，非 QWERTY 布局不受影响（#63）。
- 一键创建工作项组不再包含 Epic/Initiative 顶层，自动层级从 Feature 层起步；Task 仍拆【开发】/【测试】（#65）。
- 跨会话转发带附件的消息时，受保护文件下载后重传到目标会话：单文件消息文字作为文件说明、结果仍是一条消息；重传前校验服务器上传上限，单个文件失败退回原「请在原会话查看」提示。合并转发保持原提示行为（#69）。

### 安全

- 工作区配置文件按不可信输入处理：非 http(s) 地址、URL 内嵌用户名密码、携带 key/pat/token 字段的一律拒绝导入；PAT 与 AI 密钥只存本地，永不被导入改动。
- 看板拖拽改状态先弹确认再写入，只更新 System.State 一个字段。

## v0.20.1 - 2026-07-17

### 变更

- Codex 成为默认聊天与本地工作入口：独立入口和共享话题 Agent 都直接在用户选择的本地目录启动 `codex app-server`，不再要求安装、构建或运行 Agent Runner Docker 镜像。
- AI 助手的明确搜索、查询和工作项草案指令继续本地解析；模糊自然语言改用带 JSON Schema 的 `codex exec` 做白名单意图路由，DeepSeek 降为总结、晨报和文本能力的可选 Provider。
- 共享 Agent 默认复用 Codex 入口已配置的工作目录；临时附件通过额外运行时根提供，工作区写入仍受 Codex 原生 sandbox 与 RocketX 审批限制。
- 删除 Agent Runner Dockerfile、安装设置、镜像 smoke 和每次 PR 重建镜像的 CI 步骤；Rocket.Chat、MongoDB 与 RocketX Web 的可选自部署 Compose 保持不变。

### 安全与兼容

- 本地目录现在是真实的 Codex 工作目录，不再映射为容器内 `/workspace`。用户应只选择允许 Codex 读取的目录；网络默认关闭，写入模式只授权当前工作区。
- Codex app-server 改为校验 CLI 与握手版本一致，并对 RocketX 实际使用的方法做运行时结构校验；协议差异审计使用当前 CLI 实际生成结果，不再因兼容补丁版本直接拒绝启动。
- Windows 桌面端会解析 npm 安装生成的 `codex.cmd`，并通过官方 Node 入口启动 Codex；版本探测、管家 app-server 与 `$codex` 单轮通道复用同一入口。
- 管家设置新增可持久化的 Codex 模型与推理强度；模型留空时沿用 CLI 默认值，配置变化会在下一问前重建常驻线程。

## v0.20.0 - 2026-07-17

### 新增

- 新增独立一级入口「AI 助手」：DeepSeek 优先把自然语言路由为跨消息、会话、联系人、待办、日程和 ADO 数据搜索，也可查询工作项、PR、构建并生成工作项创建草案；Provider 不可用时显式命令安全回退到本地规则。
- 新增独立一级入口「Codex」：桌面端可选择并按账号记住本地工作目录，新建或恢复隔离的 Codex 会话，查看回复与本地过程，并在页面内审批命令、文件变更和增量权限。
- 完成 M11 应用生态：`@rcx/app-sdk` 成为 manifest 与 Bridge 的公开类型契约，`create-rcx-app` / `rcx-app` 提供脚手架、校验和回环地址开发预览。
- 官方样板扩展为看板、投票和值班表；全部只使用公开能力和最小权限，不依赖宿主私有 API。
- 新增固定版本的一键 Web 自部署栈，包含 RocketX、Rocket.Chat 8.6.1 与 MongoDB 7.0.21，并提供同源 API、WebSocket、文件和头像反代。
- 补齐英文主 README、中文入口、应用开发、兼容矩阵、贡献、安全与第三方许可文档。

### 发布工程

- 统一仓库、Web、桌面、SDK、CLI 与桥接服务版本为 0.20.0，并把 Codex 0.144.4 的 671 个协议生成文件纳入 CI 一致性门禁。
- 三平台 Release 工作流先执行协议、类型、测试、应用生态和 Web 构建门禁，再核验 Windows、macOS、Linux 产物、Updater 元数据与签名，并生成可直接校验的 `SHA256SUMS.txt`。
- 标签只能来自版本完全一致的最新 `main`；外部开发者 30 分钟验收与真实 README PNG/GIF 仅在进入 1.0 及以上时成为强制成熟度门禁。桌面构建只准备草稿，npm 与 GitHub Release 均由受保护的独立工作流显式发布。
- 本地 clean-room 应用生态、三服务 Docker、真实 Rocket.Chat、Codex app-server、Agent Runner、Rust 和 Windows Release 已通过；v0.20.0 正式标签仍以三平台 CI 和 npm 包公开可见为发布门禁。

### 安全

- AI 助手的只读查询可直接执行，工作项创建只生成草案并复用既有创建窗口做最终确认；模型不能直接写入 ADO。Codex 继续只挂载所选目录，默认只读，敏感路径、网络权限和未展示路径的文件变更保持拒绝。
- CLI 开发服务只绑定回环地址；应用 manifest、entry 路径和权限继续由 SDK 单一解析器校验，不为开发模式放宽宿主 CSP。
- 发布确认输入只通过工作流环境变量进入 shell；同版本 npm 包已存在时必须与标签提交一致，避免重跑覆盖或命令注入。
- Compose 明确定位为本地/评估基线；生产部署仍必须配置 TLS、强凭据、网络收敛与备份恢复。

## v0.19.0 - 2026-07-17

### 新增

- 完成 M10 IP Messenger 共存模式：桌面端按需监听 UDP/TCP 2425，支持成员发现、普通消息确认重试和普通文件拉取。
- 消息侧栏新增独立的本地兼容频道，可选择在线 IP Messenger 联系人发送消息或文件；局域网消息同时进入「今日」收件箱。
- 设置页新增默认关闭的兼容模式开关、端口冲突错误和在线联系人状态，不影响 M9 可信 LAN。
- 原生协议支持标准 UTF-8/CP932 与飞秋 GBK fixture；官方 IP Messenger 5.8.3 已完成真实发现、消息和文件互通验收。

### 安全

- IPMSG peer 明确标记为未认证旧协议身份，不复用 M9 固定公钥、可信设备或 Rocket.Chat 会话动作。
- 收到的文件必须由用户显式下载，只写入应用数据目录；文件名拒绝路径穿越，临时文件完整接收后才发布。
- 飞秋公开二进制因无签名且当前宿主缺少可用隔离环境而未执行；按蓝图收缩为原版 IP Messenger 真实互通，不虚报飞秋运行时兼容。

## v0.18.0 - 2026-07-17

### 新增

- 完成 M9 可信局域网：mDNS 主发现、TTL=1 的 UDP 组播兜底、Rocket.Chat 认证通道设备公钥交换，以及 Ed25519 固定公钥双向挑战应答。
- 桌面端文件发送在可信单人会话中自动使用原生 TCP；固定 1 MiB 分片、最多四路并发、缺块续传、分片和整文件 BLAKE3 校验，并显示实测速率。
- Rocket.Chat 不可达时，普通消息可经可信 LAN 投递；作者使用稳定消息 ID 从 IndexedDB outbox 幂等回灌，参与会话的设备保留原始时间。
- 应用能力总线新增 `lan.peers` 与 `lan.send`，分别受 `lan:discover`、`lan:transfer` 权限控制；peer 输出不包含 IP、端口或设备公钥。
- 桌面设置新增 LAN 与离线回灌诊断，明确当前可信设备数量及服务器消息自定义字段能力。

### 安全

- 广播中的 userId、公钥和地址永远只作为发现候选；未在 Rocket.Chat 认证会话中固定公钥的设备不能建立 LAN 连接。
- LAN 文件名拒绝路径穿越，接收文件只写应用数据目录；每片校验后写入临时文件，整文件校验通过才发布。
- Rocket.Chat 回退上传前读取服务器文件大小上限，避免把超限大文件整体载入 WebView 内存。

## v0.17.0 - 2026-07-17

### 新增

- 完成 M8 共享 Agent：Rocket.Chat 线程直接承载 Codex 长会话，支持会话租约、恢复、串行命令队列、成员加入审批和本地过程面板。
- Agent 上下文新增引用消息整条线程、参与者、关联 ADO 工作项和只读临时附件；宿主可切换“仅自己”并用 `/exit` 结束会话。
- 接入锁定版本的 Codex app-server 协议，生成类型、真实 stdio 握手、流式回复、服务端主动请求与进程中断均由薄适配层处理。
- 新增只读优先的工作区安全边界、显式敏感路径请求拒绝、输出密钥脱敏和危险动作宿主审批。
- 新增每会话独立的 Docker Agent Runner；仅挂载当前工作区、只读上下文附件和只读 Codex 认证文件，支持状态诊断、一键安装与容器终止后的 thread resume。
- 新增可选 Agent Bot 身份；凭据仅存系统凭据库，且只能向配置所属的 Rocket.Chat 服务器发送。
- 新增反向 MCP stdio 入口，向外部 Agent 提供会话列表、线程上下文和房间历史三个只读工具。

### 安全

- Codex 默认运行在固定 Linux Runner 的只读权限配置中；`.env`、密钥目录和认证文件由 bubblewrap 拒绝，写工作区、执行命令及其他高影响请求仍须当前宿主批准。
- 反向 MCP 与 Agent Bot 的 Rocket.Chat token 不接受命令行、环境变量或明文配置文件输入，状态接口也不返回 token。

## v0.16.0 - 2026-07-17

### 新增

- 完成 M7 AI 管家：新增 OpenAI-compatible、Anthropic 与 Azure OpenAI Provider，总结、结构化提取、晨报/晚间回顾统一走可按能力路由的 AI 总线；DeepSeek V4 已完成实网验收。
- 新增统一收件箱「今日」，聚合直接 @我、到期/逾期待办、今日日程和分配给我的 ADO 工作项，支持处理进度、原上下文跳转及 AI 回顾。
- `/summary` 可无副作用分页收集完整未读历史；消息操作可让 AI 预填待办或 ADO 工作项。
- Ctrl+K 新增语义搜索模式，向量索引按服务器、账号、成员房间及 embedding 模型隔离，并支持增量更新和模型变化重建。
- 通知默认按会话聚合，直接 @我、P1 与紧急关键词可配置穿透；内置基线/dogfood 统计，衡量每日通知弹窗下降比例。
- 新增 `$codex` 单轮快速通道，以只读、临时沙箱执行本机 Codex CLI，并把完整结果回帖到当前会话。

### 安全

- AI 密钥仅保存在操作系统凭据库；网页环境只提供当前会话内存回退，不写入 localStorage 或 IndexedDB。
- 每项 AI 能力可独立限制为本地 Provider，调用结果写入不含提示词和密钥的本地审计日志。
- `$codex` 提示词只通过 stdin 传递，命令参数固定为只读与临时执行，并设置五分钟超时。

## v0.15.0 - 2026-07-17

### 新增

- 落地 M6 扩展内核：支持 12 类扩展点声明，首批打通导航模块、右侧面板、消息动作与渲染、斜杠命令、输入触发和实体链接。
- 新增 `@rcx/app-sdk` 与 `@rcx/rcx-store`，提供 JSON-RPC Bridge 客户端、IndexedDB 应用分区存储、审计、向量和离线队列基础。
- 应用设置页支持本地目录与 URL + SHA-256 安装、启停、敏感权限编辑和卸载；随仓库提供 Hello 与消息看板样例。
- 接入 Tauri updater + process 插件、GitHub Releases 更新源、签名产物和更新后重启流程。

### 安全

- 主窗口 CSP 收紧，iframe 使用无同源权限的 `sandbox="allow-scripts"` 与独立 CSP，Bridge 改为一次性 `MessageChannel`。
- 移除全盘文件和静态宽 HTTP 授权；宿主 HTTP 请求按精确 origin 动态授权，应用出网额外受 `netAllow` 白名单与权限闸门约束。
- 远程应用不得申请 `agent:spawn` / `process:spawn`；所有能力调用均先鉴权并写入本地审计日志。

## v0.14.11 - 2026-07-17

### 新增

- 消息搜索提供明确的“搜索全部”入口；需要完整历史时可主动扩大到所有会话，并继续向下滚动加载更早结果。

### 优化

- 输入关键词时默认先搜索本机已加载消息和当前会话，避免每次搜索都立即扫描全部会话，降低等待时间和 Rocket.Chat 服务端压力。
- 搜索范围、加载进度和完成状态会直接显示在面板中；修改关键词后自动回到轻量搜索，不会沿用上一次全量范围。

### 文档

- 产品蓝图更新为 v2，以 GTD 与注意力保护为双支柱，重新定义 M6~M11、稳定化轨道和开源发布门禁。
- 仓库采用 MIT License，并将开发环境的 Rocket.Chat 镜像固定为已验证的 8.6.1，避免新环境因 `latest` 漂移而失去可复现性。

## v0.14.10 - 2026-07-17

### 优化

- 消息搜索首批显示 20 条，滚动到底后自动加载下一批；服务端全局搜索按 20 条递增请求，逐会话回退使用 `offset` 分页，不再把 20 条作为总结果上限。
- 搜索分页结果缓存从最近 20 个查询收敛到 5 个，超过 200 条的首批结果不跨查询缓存，避免单次查询结果增多后放大内存占用。

### 修复

- 修复 Windows 桌面端导出诊断日志时被 Tauri 文件命令 ACL 拒绝的问题；继续沿用现有文件写入权限，不扩大文件系统授权范围。

## v0.14.9 - 2026-07-16

### 优化

- 全局搜索会先从本机已加载消息中立即返回结果，再按最近会话优先分批补全完整历史，不再等待所有会话搜索结束后才显示消息。
- 搜索状态分别提示联系人/频道和完整历史消息，远端历史搜索失败时仍保留本机已找到的结果。

### 修复

- 已退出或已删除的私有会话不会通过本机消息缓存重新出现在搜索结果中。

## v0.14.8 - 2026-07-16

### 新增

- Azure DevOps 工作项列表支持按当前查询结果中的父子关系分层显示，并可逐级展开或折叠；搜索和状态筛选会保留命中项的祖先路径。

### 修复

- 修复从搜索结果或通知定位过旧消息后，重新打开会话可能先滚到最新消息、随后又向上回弹的问题。
- 修复 Azure DevOps 树查询中重复关系可能占用结果上限、漏掉后续唯一工作项的问题。

## v0.14.7 - 2026-07-16

### 修复

- 修复部分 Rocket.Chat 服务器未开启全局消息搜索时，快捷搜索会把服务端空结果误判为“没有相关消息”的问题。
- 全局搜索不可用时自动低并发查找全部可访问会话的完整历史，只在客户端保留最近 20 条候选；相同范围的成功查询会短时复用，兼顾搜索完整性与资源占用。

## v0.14.6 - 2026-07-16

### 修复

- 修复打开或重复点击会话时，消息列表可能在布局稳定前完成首次滚动，导致没有停在最新消息的问题。
- 消息内容异步撑高、输入区或窗口改变滚动视口高度时，继续保持最新消息可见。

## v0.14.5 - 2026-07-16

### 改进

- 长时间运行时只完整保留当前和最近使用的会话数据，较早会话保留消息预览并在再次打开时重新加载；Azure DevOps 悬停查询也增加容量和时效边界，减少持续增长的内存占用。
- 通讯录、成员列表和搜索结果中的离屏头像改为接近视口后再加载，头像与聊天图片使用独立缓存，减少无效请求和相互淘汰。

### 修复

- 修复从消息切到大通讯录再返回后，常用头像可能变成空白或需要滚动才重新出现的问题。
- 修复切换 Rocket.Chat 服务器或账号时，旧会话迟到的历史、角色或回执响应可能写入新会话状态的问题。

## v0.14.4 - 2026-07-16

### 新增

- 隐藏会话增加独立分组，可直接恢复并打开，不再只能等待新消息重新出现。

### 改进

- 默认发送方式改为 Enter 发送、Alt+Enter 换行；设置中的 Ctrl+Enter 发送模式继续保留。
- 多选态增加明确的消息勾选按钮，批量删除只处理自己的消息并按顺序完成。
- 消息操作统一显示“删除”，不再按消息时间在“撤回”和“删除”之间切换。

### 修复

- 修复多选消息点击不稳定，以及并发批量删除可能让已删除消息重新出现的问题。
- 修复引用图片在 Rocket.Chat 服务端展开为嵌套附件后不显示的问题。

## v0.14.3 - 2026-07-16

### 新增

- 消息多选工具栏支持直接复制、逐条转发、合并转发和 Markdown 导出；文字、图片与文件说明共用同一输出规则。
- 通知设置增加独立的任务栏窗口闪烁开关，不再与托盘图标闪烁混为一项。

### 改进

- 多选消息默认使用逐条转发，完整保留 Rocket.Chat 原生图片和文件结构；合并转发作为可选操作且不再附带原发送人信息。
- 全局搜索的“全部”结果把联系人和频道置于最上方，减少从搜索发起会话的操作距离。
- 桌面头像 objectURL 缓存限制为 128 项并安全回收未使用资源；切换会话时只保留当前房间的实时订阅，降低长期运行的 WebView2 内存与事件分发压力。

### 修复

- 修复多选状态提示可按 Esc 取消、但实际没有响应 Esc 的问题。
- 修复附件消息无法通过单条“复制”操作输出内容的问题。

## v0.14.2 - 2026-07-16

### 改进

- 增加 Windows 桌面端 30 分钟内存趋势验收，统一统计 RocketX 与 WebView2 子进程的私有内存、峰值和增长，并输出 CSV 供回归比较。

### 修复

- 修复点击消息通知只打开会话但不能定位到对应消息，以及重复点击当前会话时仍停留在较早历史位置的问题。
- 修复群组头像未使用 Rocket.Chat 房间头像、切换服务器或账号时可能短暂复用旧头像，以及头像加载期间出现空白的问题。

## v0.14.1 - 2026-07-16

### 改进

- 托盘悬停时展示未读会话数、未读总数和分会话摘要；通知设置中可关闭托盘图标闪烁，同时保留未读数字。

### 修复

- 修复 Windows 托盘闪烁时透明帧可能显示为黑色方块的问题，改为保留透明通道的明暗交替。
- 修复引用回复发送后被即时响应覆盖、只在发送前界面短暂显示引用内容的问题。

## v0.14.0 - 2026-07-16

### 新增

- 新用户可通过可恢复的首次引导完成 Rocket.Chat 登录和 Azure DevOps 配置；ADO 暂不可用时可跳过，进入主界面后继续完成会话、消息与通知清单。
- Windows 全局快捷键可从任意位置打开统一指令中心，优先处理未读，并搜索会话、消息、联系人、文件和工作数据。
- 搜索支持按发送人、时间和文件类型筛选；常用文件使用有边界的本地索引，不需要逐个会话发起请求。
- Windows 桌面端支持开机自启、单实例唤回、通知点击直达会话，以及脱敏的本地诊断日志导出。
- 会话列表宽度、分组栏和高频快捷键可按个人习惯调整，并支持连续跳转到下一条未读。

### 改进

- 通知权限改为用户主动启用，不再在登录后静默申请；首用清单可永久跳过且不会重复提醒。
- 创建 Azure DevOps 工作项时会记住当前账号和连接最近选择的项目，复用现有层级模板。
- Azure DevOps PR 卡片展示发起人、评审进度和服务端标记的必审组；“我提的 PR”只显示活动项。
- 全局搜索优先使用 Rocket.Chat 服务端能力，并在不可用时以受控并发回退，避免请求风暴和旧结果覆盖新查询。

### 修复

- 修复切换 Rocket.Chat 服务器或账号后，引导、布局和 Azure DevOps 项目选择可能串用的问题。
- 修复桌面通知导航接受无效房间标识、重复启动产生多个窗口，以及较早搜索请求覆盖当前结果的问题。

## v0.13.0 - 2026-07-16

### 新增

- Azure DevOps PR 与构建链接可在消息中直接展开为摘要卡片；加载失败时仍保留可点击的原链接。
- Windows 托盘图标会在有未读消息时闪烁，并在未读清零、退出或重载时可靠恢复。

### 改进

- 层级工作项按 Azure DevOps Server 的真实过程配置生成，兼容 Basic、Scrum、CMMI 与自定义流程。
- 未加入的公开讨论仍可查看和发送消息；加入后才启用订阅通知与已读同步。
- 首次打开会话默认滚动到最新消息，未读分割线仅作视觉提示。
- 任务栏未读提醒不再依赖桌面通知开关，并继续遵守免打扰与当前窗口焦点状态。

### 修复

- 修复公开讨论误用频道加入接口导致 `error-room-not-found` 的问题；旧版服务端仅在 `rooms.join` 返回 404 时回退 DDP。
- 修复已有讨论订阅未及时刷新、进入讨论后未读无法清除，以及未订阅讨论错误触发通知的问题。
- 修复分页首条被误判为未读边界、托盘异步旧帧覆盖正常图标等竞态。
- 78 项专项回归纳入持续集成，覆盖 ADO 卡片、讨论访问、初始滚动、任务栏与托盘状态。

## v0.12.1 - 2026-07-15

### 改进

- 快捷搜索不再因空结果或实时会话更新向 Rocket.Chat 连续发送逐房间请求，并限制回退搜索的并发量。
- 通讯录与房间成员分页增加人数、页数和无进展保护；多个面板同时读取成员时复用同一请求。
- 46 项并发、分页、信任边界与状态隔离回归测试纳入持续集成。

### 修复

- 修复快捷搜索无法按拼音找到中文联系人，以及较早搜索结果覆盖当前关键词的问题。
- 修复通讯录重复页、成员列表重复页或异常总数可能造成长时间请求和残缺结果的问题。
- 修复切换房间后旧成员请求回写当前面板，以及邀请成员时旧快照覆盖新名单的问题。
- 修复切换 Azure DevOps 服务器时旧刷新覆盖新配置、数据和错误状态的问题。
- 修复切换或清空 Azure DevOps 连接后，工作项链接仍可能指向旧服务器的问题。
- 修复多个 Azure DevOps 自定义查询共用加载、错误和缓存状态的问题。
- 修复自定义 Azure DevOps 查询跨账号或服务器复用、外部伪装链接可被保存，以及升级迁移可能误删其他服务器旧查询的问题。
- 修复邀请群外成员期间切换会话时，消息可能被发送到新会话的问题。
- 修复搜索关键词变化时短暂显示上一关键词结果，以及部分重复分页静默缺少用户的问题。
- 修复快速变化未读数时，较慢的旧任务栏角标最后覆盖最新值的问题。

## v0.12.0 - 2026-07-15

### 新增

- 快捷搜索同时覆盖会话、跨会话消息、联系人和频道，并支持中文拼音与备注名。
- 通讯录可完整加载 Rocket.Chat 用户目录，成员与文件列表支持服务端分页。
- 从聊天创建 Azure DevOps 工作项时，按项目过程动态加载真实工作项类型。

### 改进

- 优化消息列表滚动锚点、窄窗口布局、线程与侧栏高度、PDF 预览资源回收。
- 完善合并转发和跨房间附件处理，保留可复用内容并阻止原房间受保护文件链接泄露。
- Azure DevOps Bridge 自动识别当前身份，并按当前用户筛选工作项、PR 与构建。
- 统一模态弹窗的 Esc、焦点陷阱和焦点恢复行为。

### 修复

- 修复 Rocket.Chat 文件重定向到外部地址时可能继续携带认证头的问题。
- 修复成员邀请后被较早请求的旧名单覆盖，以及刷新失败时无法可靠重试的问题。
- 修复 Azure DevOps Server 2022 不存在的截止日期字段导致 Bridge 请求失败的问题。
- 修复工作项类型加载失败后仍可使用陈旧类型创建、错误配置被误报为连接成功的问题。
- 修复实时消息更新反复重启快捷搜索，以及创建工作项过程中关闭弹窗可能造成重复提交的问题。
- 修复收藏链接协议校验、弹窗键盘操作、窄视口和多项工作台数据边界。
